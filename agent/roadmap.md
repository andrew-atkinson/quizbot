# coursekit roadmap

The forward-looking view.
For the detailed record — measurements, dated decisions, per-course results — see the working log (kept offline).
This file is the map; the log is the diary.

## Vision

> coursekit turns a course's own material into Canvas-ready artifacts that are **correct, well-taught, and reviewable** — locally, and measurably.

Every decision checks against that line: does it make artifacts more correct, better-taught, more reviewable, or the tool more usable to a real instructor?
If not, it goes to the parking lot.

## How we work (the cadence)

1. **One active focus at a time.** A single _current milestone_ (below). Everything else waits.
2. **Parking lot.** A new idea gets written down immediately so it is never lost — and is **not acted on** until a regroup. Ideas are welcome; acting on them mid-focus is not.
3. **Every milestone has a "done when."** A concrete, ideally measurable acceptance (e.g. "recall > X", "5/5 discrimination", "the review runs from one command"). No open-ended work.
4. **Regroup at milestones.** When the current thing lands: review the parking lot, re-prioritise, pick the next focus. Only then.

## Current focus

> **Between focuses — regroup.** No single active milestone right now: the last stretch shipped the concept map / `analyze` phase, the targeted fix loop, and images-on-pages (see **Shipped**). The strongest candidates for the next focus, from the parking lot: the **visual / design-forward track** (next concrete build: generated diagrams), and **composable generation** (the attention/crowding answer — validate empirically first with an A/B, don't rewrite blind). Pick one deliberately.

## Workstreams

Four workstreams. Each: the goal, where it stands, and the next move.

### 1. Generate — artifacts from a course's material

- **Goal:** good quizzes and pages (later: assignments, modules, discussions, rubrics) from any course.
- **Status:** quizzes + pages solid — canonical IR (`bank.json` / `page.json`), hardened tool-call loop, design system, emitters. This is the mature, steady core.
- **Next:** steady; new generators are a breadth decision, not a current need.

### 2. Evaluate — measure quality, and improve from it

_(Evaluation exists to improve; the loop-back is its point — so "improve" lives here.)_

- **Goal:** trustworthy, measurable quality gates that raise generation quality.
- **Status:** **all three checks built + calibrated** —
  - _Facticity_ (is it correct?): quiz + page critics, domain-aware; ~98–100% recall / 0% false-flag on the synthetic sets; validated on a real course.
  - _Form_ (does it scan/signal/engage/retrieve?): the pedagogy rubric; 5/5 discrimination; drove a real prompt loop-back (course pedagogy 9.25 → 13.5 / 15).
  - _Concept delivery_ (does it actually teach the concepts?): v1; 3/3 discrimination with graded middle.
  - Backed by synthetic calibration infra (`synthesize`, `scoring`, the scorecards).
- **Next:** the loop is closed — `evaluate --all` reports all three checks and `fix` repairs flagged items in place. Parked deepenings: the coherence/development-adequacy dimension and the visual (render-and-see) pass.

### 3. Profiles — work across domains and teaching styles

- **Goal:** the tool adapts to different subjects and different instructors, composably.
- **Status:** the `domain.md` profile exists and now feeds both generators _and_ critics. Everything else is a parking-lot idea.
- **Next:** (parked) composable domain bases + a domain-suggester; pluggable pedagogy strategies; a voice/tone profile.

### 4. Deliver & course-scope — into Canvas, and across weeks

- **Goal:** artifacts land in the LMS; the tool can reason about a whole course, not just a week.
- **Status:** file delivery solid (QTI `.zip`, HTML, CC, whole-course `.imscc` — verified in real Canvas). Course-level reasoning and any API/extension path are unbuilt.
- **Next:** (parked) Canvas API/extension; a course-level evaluator (pacing, spacing, sequencing); the front-end / form-factor track.

## Parking lot

Captured, deliberately not active.
Reviewed at each regroup.
`[theme]` tags where it belongs.

- **assignments and discussions** need to be added.
- **Quiz introduction** needs reworking. Very generic and factual but doesn't give a good reason for taking the quiz (other than it being mandatory). Need to explore more engaging quiz forms within the remit of canvas.
- **A different assessment instrument for ENDURING UNDERSTANDINGS** `[generate/evaluate]` (user, 2026-08-02) — enduring understandings are inherently hard to assess with multiple choice, and especially badly served by it: MC rewards recognition of discrete facts, but an enduring understanding is a transferable, meaning-making insight (the UbD macro-level above the concept map). Forcing it into an MC group yields vague or mis-formed stems — **confirmed this session**: the `enduring_understanding` groups drew a disproportionate share of the quiz flags (`g6/A` "grammatically incomplete/nonsensical", etc.), and the flexible-quiz-count feature currently makes one MC group *for* the enduring understanding, which is exactly the thing MC can't test. This is authentically UbD (Wiggins & McTighe pair enduring understandings with **performance-based** assessment — the six facets / GRASPS tasks — not selected-response). **Direction:** a distinct assessment type keyed off each week's `enduring_understanding` (already in the concept map), producing an open-ended task + rubric rather than `bank.json` MC variants. Seed ideas from the user (grist, not exhaustive): **Performance Tasks** — solve a real problem using the knowledge; **Transfer Goals** — apply the core idea to a completely new situation; **Explanation & Defense** — teach the concept or explain in their own words why it matters. Implications: it's a new artifact/emitter (task + rubric), not an MC bank; open responses are **instructor/human-graded** — coursekit produces the task and rubric, never grades student work (keeps the [instructor-content-only] boundary). Near-term option while this is parked: stop minting an MC group for the enduring understanding (make it optional / drop it) so the quiz stops carrying the question type it's worst at. Relates to the concept map's `enduring_understanding`, [[assignments and discussions]], and the quiz-voice flag-rate finding.
- **Studio, Bauhaus and Plotter Styles** they add bullet points on line breaks in the comparison columns, but with code that looks wrong. Also no indents preserved. ![unwanted bullets](<screengrabs/Screenshot 2026-07-29 at 3.32.33 PM.png>) Needs a better way of displaying comparisons other than bulletted lines.
- **Concept-delivery v2** `[evaluate]` — pin concept _extraction_ to the material (counts wobble now); optional course-authored concept list.
- **Multi-model reads** `[evaluate]` — for borderline facticity cases the single critic is a coin-flip; a second _model_ (not seed) would let the union help. Seed-diversity is a proven no-op on the local model.
- **Evaluator blind spot: quality vs presence** `[evaluate]` — a real week-7 "details" Q&A (a muddled, partly-wrong retrieval prompt: spurious array framing; `undefined` vs `NaN` confused) passed all three checks. Two gaps: (1) the pedagogy rubric scores a hook/retrieval/contrast for _existing_, not for being _good_ — a present-but-hollow device still gets 3/3, and the loop-back added more such devices; (2) facticity misses a _near-miss_ — a muddled-but-on-topic answer (right vocabulary, wrong reasoning) reads as plausible. Human review still catches these; the critic is a first-pass filter, not a replacement.
- **Development-adequacy / coherence dimension** `[evaluate]` — the metrics reward structural _presence_ (headings, a retrieval prompt) and _nominal_ coverage, but miss over-compression: week-7 crams functional-abstraction → arrays → array-manipulation → arrays-of-objects → parallax with no transitions and no concrete array example, yet scores pedagogy 14/15 and concept-delivery 3/3. Add a dimension for whether dense content is adequately unpacked, connected, and load-managed (CLT: intrinsic load, segmenting, coherence). The current rubric only faintly sensed it (week-7 SIGNALING 2/3, "the one key idea is diluted").
- **Visual pass of the rendered page** `[evaluate]` — the critic reads block _text_ and never SEES the page, so a structurally broken page (week-7 `--detail full`: empty trailing headings, unreadable near-white text) passed facticity with **0 flags**. Closing the blind spot means rendering the page and evaluating it visually/structurally — a vision model on the HTML, or at minimum structural checks (empty headings, contrast/WCAG, block ordering, coherence). The concrete shape of "judge coherence, not just content."
- **Pedagogy rubric — more dimensions** `[evaluate]` — fold in representation / accessibility / restraint; sharpen the 0–3 anchors; more-extreme calibration variants.
- **Retire the hand-authored synthetic set** `[evaluate]` — the generated set scores cleaner; consider making `synthesize.py` the single source.
- **synthesize.py for GENERATION** `[generate]` — the programmatic generator may make _better_ questions than the model path; explore using it for generation, not only eval. (Look closely, deliberately parked.)
- **Visual / image-bearing question types** `[generate]`.
- **Composable generation — the attention/crowding answer** `[generate/arch]` (user, 2026-07-31) — instruction crowding is a first-order, GROWING concern: the monolithic single-shot page generation overloads a small model's finite attention, and every capability we add (images, concept checklist, contrast, one-idea) taxes the same budget. **Already biting** — the same root cause explains the fading retrieval foldouts, errors clustering later in a long generation, and the `--detail full` runaway. **Direction:** decompose generation into focused sub-tasks, each with a tight instruction set + small context, so crowding can't happen — and the **concept map is the natural decomposition seam** (it is already the plan): one pass for the frame (hook + enduring understanding + recap from prereqs), one focused pass PER concept (its material + KCs + "teach this one thing well"), one for the close (summary + retrieval), then a light assembler. **Two separable axes:** (1) decompose the GENERATION (attacks attention); (2) emit MULTIPLE pages per week — per concept or grouped — which segments pedagogically (CLT), aids navigation, AND is authentic to the real course (the ARGS260 module is many wiki pages, not one giant page); generating per-page gives both benefits at once. **Validate empirically first (the project's calibration discipline):** an A/B — same week generated monolithically vs concept-map-decomposed — scored by the three existing evaluators (facticity error rate, pedagogy RETRIEVAL, concept-delivery). That measures whether decomposition lifts quality before a big rewrite. **Cost:** a real architecture change (Generator seam runs one conversation per unit today; this needs several sub-conversations + an assembler + a coherence/through-line layer, more model calls but each smaller/faster/higher-quality). This is the scaling answer for staying quality-competitive on LOCAL models as the tool gets richer. **Sharpened 2026-08-04 (session evidence + an assessment plan before the rewrite).** Separate TWO burdens, because they have different fixes: (1) **instruction load** — how many standing rules + how much transcript sit in the prompt (domain, voice, concept checklist, images, contrast, retrieval gate all compete for the same attention); (2) **output scope** — how much the model must emit in one uninterrupted pass (a whole page of N sections, a whole bank of N×4). Reducing (1) = trim/route instructions; reducing (2) = decompose the output; do both. **Fresh evidence it's biting now, not later:** the page heading-order break (all headings dumped at the end, appeared only once voice added load — the model shed a hard structural rule); the quiz form-mismatch (a conceptual "why/benefit" stem paired with code-snippet options) that PERSISTED across regens; the hook-anchoring (latching onto the one in-prompt example = load-shedding); and a **negative A/B** — scoping voice out of quiz stems did NOT lower the flag rate, so voice was not the driver, which itself argues the burden is STRUCTURAL (juggling the position rule + type mix + coverage + distractor plausibility at once) rather than any single instruction. **Modularization levers beyond per-concept passes:** (a) **route** instructions to only the blocks that need them (voice→prose not code, contrast→only a wrong-vs-right, retrieval→once at the close); (b) **move deterministic work out of the model** — the "half must be MC" type mix (assign up front), the correct-index rotation (shuffle options to the target index post-hoc), section assembly (the model already fails it), glossary/key-terms placement — every deterministic thing removed frees attention for the only thing the model is needed for, judgment; (c) **feed sharper material** — a concept's own slice/KCs, not the whole raw transcript. **How to assess what context earns its place (measure, don't guess) — the four probes:** (1) **ABLATION via the run-store** — remove one component, regenerate, measure the flag/pedagogy delta; removal that doesn't hurt = dead weight, removal that helps = active crowding (the voice scoping was an informal ablation, and it came back negative); the archived `evals/` runs are the substrate. (2) **ERROR-POSITION analysis** — flag rate vs. block index (from `calls.jsonl` order + the review files); quality decaying with position ⇒ the output is too long for one pass ⇒ decompose (tests burden #2); cheap, no model needed, runs over existing runs. (3) **INSTRUCTION-ADHERENCE probes** — how often each hard rule (heading interleave, index rotation, option-type match) is obeyed early vs. late; a rule kept at the top and dropped at the bottom is being crowded out, and names WHICH instruction is fragile. (4) **TOKEN accounting** — the prompt's share that is transcript vs. standing instructions vs. domain vs. voice vs. concept map; shows the shape of the burden. **Plan: measure before rewriting.** Run ablation (#1) + error-position (#2) first (cheap; #2 needs no model and works on runs already generated), harvest the deterministic wins now (type mix / index rotation / assembly — safe and immediate), THEN commit to the per-concept decomposition with numbers in hand rather than a blind rewrite. The coherence/through-line layer is the real cost of decomposition (a per-concept pass loses the global view). Local-specific: a frontier model would swallow the monolithic prompt; a ~26B local model has a fixed attention budget, so this is the scaling path for staying local-first as capabilities grow, not a tidiness nicety. **Probe results 2026-08-04 (evals/errpos.py + evals/tokacct.py, no model).** _errpos_ (output scope): too few archived flags to conclude (9), one weak lean toward late-group elevation that survives the enduring-understanding confound. _tokacct_ (instruction load) is more decisive — the generation prompt is **80–92% transcript** (quiz avg 86%, page 80%); shipped instructions are only **7–12%**, voice **4%**, domain **1%**, concept map **2%**; and the instruction share SHRINKS as the transcript grows (week-3 33K chars → 15% instructions; week-8 128K → 4%). Two conclusions: (1) **trimming/routing instructions is a small lever** — the rules are already a sliver; cutting them barely moves the prompt. (2) The real burden is **material volume + output scope**, so the lever is DECOMPOSITION — feed each pass one concept's slice of the transcript (the concept map already carries per-concept `key_material`), not the whole 30–128K-char week. And the small instruction share is itself a crowding argument, not a reassurance: a critical rule (interleave headings, match option type) is a needle in an 80K-char transcript haystack, easy to lose late — decomposition shrinks the haystack around each rule. Net: prioritise per-concept decomposition over instruction-trimming; the deterministic wins (move type-mix / index-rotation / assembly out of the model) still help at the margin and are safe to harvest first. **Prototype built + iterated 2026-08-04** (`coursekit/generate/page/decompose.py`, `prompts/page/{frame,concept_section,close}.md`, `--week`/`--review`/`--no-neighbours`, writes to `pages-decomposed/`): frame → per-concept → close passes into the shared IR; concept + summary headings placed DETERMINISTICALLY (the name is known — one section per concept, correct order, and the heading-order failure is impossible); a prev/next coherence WINDOW (neighbour name+gist only, not material — keeps the token win, avoids bleed); a nudge in the pass driver (the small model narrates instead of calling tools). On week-7 it went from a broken monolithic page to 56 blocks, 7 uniform sections, finalized, ~4% facticity flags. **Open boundary problem (user, 2026-08-04) — the real limitation.** The per-concept material is picked by a KEYWORD slicer (`slice_material`), which is retrieval, not SEGMENTATION: a concept's worked example usually doesn't repeat the concept's name (the code follows "let's see it:"), so the example chunk scores 0 and is dropped while the keyword-rich definition is kept — the section arrives with a definition and no demonstration, and (pre-guard) the model invented one. Contiguity is ignored too (definition + example are adjacent but scored independently). **Principled fix: stop re-deriving boundaries at generation time — have `analyze` record each concept's MATERIAL when it builds the map** (the model that identifies concepts already knows which stretch taught each: definition + example + code as one unit). Store the verbatim code/examples per concept (the fidelity-critical part keyword-slicing loses) — compact alternatives are char offsets (models unreliable at these) or a short verbatim anchor phrase per concept, spans located in order. This is the SAME artifact "Multi-source convergence" wants (per-concept material across sources), so they converge: the concept map should carry each concept's material. Cheap interim without touching analyze: make the slicer contiguity + code-aware (expand around the peak chunk to adjacent chunks; always pull in code-fence chunks near a concept's mentions) — recovers most examples, a heuristic not a solution. **Boundary segmentation BUILT 2026-08-04** (`coursekit/generate/page/segment.py`, `prompts/page/segment.md`): analyze partitions the week text into one contiguous verbatim span per concept (the model marks, in concept order, where each concept's teaching begins; the spans between marks partition the transcript), stored beside the map as `week-<key>.materials.json`; decompose reads it (`[map]`) and falls back to the keyword slicer per concept (`[slice]`). Boundary-correct + material-kind-agnostic. Assumes concepts are taught roughly IN ORDER — which is the seam for the next item.
- **Representing a non-linear / revisiting lecture in a page** `[generate/arch]` (user, 2026-08-04) — the decomposition + boundary segmenter assume concepts are taught roughly IN ORDER (one contiguous span each); a lecture that SPIRALS — teaches A, then B, then returns to A deeper — breaks that, and the current segmenter misses the later return. The open design question: how should a page REPRESENT a non-linear lecture, and it should be a CHOICE (likely instructor-selectable, like detail/theme). Options: **(a) Consolidate** — one section per concept gathering ALL its material wherever it appears (first pass + returns) into one coherent treatment; needs chunk→concept LABELING (non-contiguous gather) rather than the contiguous boundary-marking segmenter; presents each concept whole but flattens the lecture's spiral pacing. **(b) Preserve the spiral** — the page follows the lecture and a revisit becomes its own later section ("Returning to A — going deeper"); honours spiral/retrieval pedagogy (the return is intentional), but a concept then appears in multiple sections, which the one-section-per-concept model doesn't fit (needs a notion of concept "passes"/levels). **(c) Layered depth** — one section per concept, the later return folded in as a progressive-disclosure sub-block (a "Going deeper" `details`) — consolidation, but keeping the depth distinction. **(d) Graph + cross-links** — make the non-linearity explicit: sections cross-reference ("we return to this in §5") via anchors, following the concept map's prereq/`teaches_toward` edges (the map is already a graph). **(e) Detect + choose** — flag a concept whose material spans are NON-CONTIGUOUS (a revisit) and pick a structure per course/concept: merge / spiral / layer. **Mechanism note:** (a)/(c)/(e) need the segmenter to support non-contiguous per-concept material — chunk→concept labeling is the natural generalization of `segment.py`'s boundary-marking, and it also handles out-of-order teaching. Ties to composable generation, pluggable pedagogic strategies (spiral-vs-linear IS a pedagogy strategy), the concept-map-as-graph, and prerequisite gap analysis.
- **Decomposition — open questions from the first good page** `[generate/arch]` (user, 2026-08-04) — the boundary-segmented page came out well-formed, on-voice, drawing real code examples, but MUCH longer than the monolithic page (more a faithful REPRESENTATION of the lecture than a tight teaching page). (a) **Length is unwired** — the decomposer ignores the `detail` (brief/medium/full) knob; each concept pass teaches its whole span freely, so length is emergent. Decide whether "full representation" is the right default (great as a study/reference resource, maybe too long as a scannable intro) and wire a per-concept length control if not. (b) **Sub-unit granularity** — today week = one page, but the week text is a concatenation of the week's VIDEOS (the transcriber already emits per-video artifacts), so pages could decompose at the VIDEO or topic level, not just the week: authentic (the real ARGS260 module is many wiki pages ≈ per video), more navigable (one video = one page), shorter, and it naturally carries the video embed + timestamps. The unit of decomposition (week/video/concept/module) is itself a choice worth exposing — this sharpens the parked "multiple pages per week" idea to "per video." (c) **`pages-decomposed/` is a PROTOTYPE A/B location**, not production — it sits beside `pages/` so nothing is clobbered while comparing; if decomposition wins it either replaces the monolithic generator (writes `pages/`) or becomes a selectable mode. (d) **Span balance** — boundary segmentation can hand one concept a huge share of the transcript (week-8 gave "Autonomous Agents" 56K of 118K chars in ONE pass), re-approaching crowding within a single pass; a dominant span signals a too-coarse concept map (33 KCs → 6 concepts) and/or the case for per-video decomposition. Watch span sizes; balance or sub-split when one concept dominates.
- **Correctness vs style — "clunky but working" as pedagogic value, not a flag** `[evaluate]` (user, 2026-08-04) — the facticity critic flagged working-but-clunky code (a `move()` method that also draws — should arguably be `update()` or split) as "incorrect". It is NOT wrong, so it is a false positive on correctness — but the observation is pedagogically REAL. Two moves: (1) tighten the facticity critic to separate INCORRECT (won't run / contradicts the material) from SUBOPTIMAL/clunky (works, not ideal), so style stops firing as a facticity flag; (2) better — capture the style observation as a NOTE rather than discarding it: an instructor-facing pedagogy note ("`move()` mixes movement + drawing; consider `update()`") or an inline student tip — turning a false positive into a feature, and honest to how teaching actually works (clunky-but-working is a teachable state, not an error). Ties to the pedagogy evaluator, the block-edit/vet UI, and the "quality vs presence" blind spot.
- **Teaching is temporal — show the settled conclusion, not a transient state** `[generate/analyze/arch]` (user, 2026-08-04) — a decomposed page reproduced VERBATIM a transient buggy code state (`fill(this.fill) // apply the gradient`, which doesn't work) that the lecture CORRECTED later in the same source (`drawingContext.fillStyle = this.fill`). The critic correctly flagged it (true positive — the net worked). Root cause: teaching is iterative (wrong→right, naive→refined) and the transcript captures every state, but a PAGE should present the settled/working conclusion, not an intermediate. The correction IS in the source, so the transcriber did not lose it — the fix is mostly downstream. Layers, cheap→deep: (a) **page-gen PROMPT** — "when an idea evolves (buggy/naive then corrected), teach the FINAL working version; use an earlier wrong version only as an explicit, labelled contrast, never as the working example" — requires segmentation to include the correction in the concept's span (may not have; check). (b) **FIX LOOP** — the critic already flagged it and the correction exists later in the material, so `fix` could regenerate the block with the corrected code (self-heal via the net that caught it). (c) **TRANSCRIBER** — the vision analysis samples every ~10s indiscriminately, faithfully recording transient states but not MARKING which is settled; it could annotate code evolution or "settle" a code block to its final form so everything downstream sees the conclusion (bigger, transcriber-side, the most principled). Read: (a)+(b) are the tractable near-term levers; (c) is the deeper improvement. A genuinely hard temporal→static translation problem, and the productive edge of the "quality vs near-miss" evaluator theme.
- **Block-level edit + re-run + a vetting UI** `[deliver/ui]` (user, 2026-08-04) — a decomposed page contained a weird, faintly patronising analogy ("keeping track of a classroom by giving every kid a notebook… you lose track"). The right answer is NOT more rules for specific analogy types (whack-a-mole); it is letting the professor VET and fix content — an edit function (with UI) and/or a per-BLOCK re-run, so a user can regenerate or hand-correct ONE block without touching the rest. The `fix` loop already regenerates a block in place; this is its user-initiated, UI-surfaced sibling. Human-in-the-loop vetting is the point; belongs in the eventual front-end.
- **Video + timestamp references on pages** `[generate/deliver]` (user, 2026-08-04) — a page should be able to reference its source videos and their timestamps (the transcriber has per-segment timecodes; a per-video page above would carry them naturally). Constraint the user raised: the page ships to Canvas, so the embed + deep-link-to-timestamp must survive the `.imscc` round trip — a real functionality limit to design against (Canvas page/file linking, iframe embeds, timestamped URLs).
- **Bundle instructor files into the `.imscc` + the manifest link↔object mechanism** `[deliver]` (user, 2026-08-04) — let a course include/upload its own files (videos, readings, images) INTO the final `.imscc`, with pages referencing them. Requires understanding how the Canvas export manifest records the relationship between a LINK and the file/object it points to — a referenced file must actually be IN the export for the link to resolve. Investigate the manifest's resource/file wiring (the real ARST260 export is the ground truth in `reference/`), then extend the CC emitter to pack supplied files + emit the manifest entries binding a page link to the packaged file. Ties to the supplements system (today only external URLs) and `emit/cc.py`.
- **Page-type taxonomy — the space of page functions** `[generate/arch]` (user, 2026-08-04) — before building more page generators, treat a page as a point in `(scope × role × source)`, not a named type (fits the flexible `page_type`). **Axes:** scope = course / module / week / VIDEO / concept / skill; role = orient (wayfinding) / frame (motivate) / teach / practice / assess / review / reference / administer; time = before (advance organizer) / during / after. **Catalog by role** (the user's six folded in): ORIENT — course home, module overview (#1), week "Start Here" (#2), course map; FRAME — advance organizer, objectives page, "why this matters", assignment brief (#6); TEACH — full week page (#5), per-video companion (#4), per-concept page, worked-example / tutorial, case study, reading guide; PRACTICE — ungraded set, "try it" interactive, lab/studio, discussion-prompt framing; ASSESS-adjacent — assignment/project guidelines, rubric-as-page, study guide, portfolio; REVIEW — week/module review (#3), cumulative review, common-mistakes / FAQ, summary diagram; REFERENCE — glossary, cheat sheet, resource list, tool-setup, examples / exemplar gallery; ADMINISTER — schedule, policies, get-help, announcements. **Canvas structural reality:** the MODULE is the real container (an ordered mix of pages / assignments / quizzes / discussions / files / external-LTI / text-header dividers); recurring macro-structures — a "Start Here" module + weekly/unit modules + a persistent "Resources" module; flipped (pre/in/post); backward design (objectives → content → practice → assess); requirement/prerequisite gating. (Moodle: sections/topics + Books; Coursera/edX: lessons of interleaved items — same idea.) **coursekit lens — cheap vs gated (the prioritisation):** the ORIENTATION / REVIEW / OVERVIEW pages are the cheapest high-value wins — low-content, high-navigation, and the data already exists (`context.yaml` has weeks/modules, concept maps have per-week topics, supplements have the links) → nearly an EMITTER/assembly job, not generation. A REVIEW page (#3) is ~free (the prior week's enduring understanding + retrieval `details`). TEACHING pages (#5 / per-concept) are the current track (need the concept map). ASSIGNMENT (#6), reading guides, and exemplar galleries are GATED on inputs coursekit does not ingest yet (assignments — see "assignments and discussions"; instructor files — see "bundle instructor files into the `.imscc`"). **Recommended next high-leverage generator: the week/module OVERVIEW pages** — near-free assembly over `context.yaml` + the concept maps, and every course needs them. Ties to composable generation (the per-video/sub-unit unit-of-decomposition question), the decomposition open-questions, and course-scope.
- **Retrieval-foldout finalize gate is PROVISIONAL — revisit/remove** `[generate]` (user, 2026-07-31) — `page.validate_final` now hard-requires a retrieval `details` block so foldouts stop being dropped. But this imposes a PEDAGOGICAL requirement to solve a TECHNICAL problem (local-model attention crowding): retrieval isn't right for every page type (a pure reference/intro page), and a hard gate can be satisfied by a hollow foldout. Keep it for now; it's a stopgap, not an invariant. Remove it (or make it page-type-aware) once composable generation manages attention properly, and if pages become multi-page/typed. Code comment marks it provisional at `page.validate_final`.
- **Images / diagrams on pages — generated diagrams (next rung)** `[generate/visual]` — instructor-supplied inline images shipped (see Shipped). The bigger step: the model AUTHORS a diagram itself (SVG/mermaid) for structural concepts (coordinate systems, data flow), no instructor file needed. Continues the visual/design-forward track.
- **Composable domain bases** `[profiles]` — ship generic bases (`computer-science`, `digital-art`, …) a course selects one or MORE of and refines; assume multi-domain courses.
- **Domain-suggester tool** `[profiles]` — scan the transcripts/ingestables → propose a starter `domain.md`.
- **Multi-source convergence for the concept map** `[analyze/arch]` (user, 2026-07-30) — a real course mixes video + readings + slides, but `analyze` today uses the RICHEST SINGLE source: if any `knowledge.json` exists (video-only — only the transcriber makes it) it builds from those KCs and never reads the week text; else it falls back to the text. So a MIXED week reflects the videos only — readings/slides ingested as text (coursekit ingest produces week text, NO knowledge.json) are invisible. Not biting yet (each of the user's courses is single-medium today), but the architecture should answer for it. Root: the two ingest paths converge at the TEXT layer (both aim at `week-N.md`) but only the transcriber produces the STRUCTURED knowledge layer. **Principled fix (same canonical-form discipline as `bank.json`, one level up):** every ingest adapter emits the SAME per-source knowledge artifact, each tagged with a `kind` (video/reading/slides), and `analyze` aggregates across all of them — medium-agnostic. The seed already exists: `build_concept_map_from_text`/`extract.md` turn text → the concept schema; run that PER-SOURCE (per document/transcript) rather than per-week-text and the existing aggregator unions it. Cheap interim: stop short-circuiting — feed knowledge.json KCs AND the week text to consolidation (but the week text isn't a guaranteed union either — ingest writes `output/week-3.md`, transcriber `output/week 3/week-3.md` — same convergence problem at the text layer). This also finally makes "the week text is the union of all sources" true at both layers.
- **Voice / tone profile** `[profiles]` (user, 2026-07-30) — the generated prose (and especially the enduring understanding, e.g. "Complexity is managed by organizing data into structures that allow a single logic pattern to govern many individual entities") reads more formal/academic than the user teaches. A THIRD composable prose layer beside domain (WHAT) and pedagogy strategy (HOW): a `.vtconfig/voice.md` (tone/register/second-person/idioms/playfulness) prepended to generation + consolidation like `domain_preface`. **Sharp insight:** the authentic voice is ALREADY in the transcripts — the formality is INTRODUCED by cleanup passes (the transcriber's format stage "fixes grammar, removes filler"; page generation writes formal written prose), sanding off the spoken register. So (a) an authored `voice.md`, and (b) a **voice-extractor** (like the domain-suggester) that characterizes the instructor's register from the raw transcripts → a starter `voice.md`, are both viable, and (b) works precisely because the voice pre-exists cleanup. User: "let's look at it sometime." **Blocks the concept-map-parametrized titles below — that idiom IS this profile.** **Boundary decision (user, 2026-08-02): the EXTRACTION belongs on the transcriber side, the APPLICATION on coursekit — a split, not a wholesale move.** Extraction (raw transcript → register features → `voice.md`) is a _source characterization_ exactly like `knowledge.json`: it must run on the RAW side of the transcriber's own pipeline, because the register lives in the raw ASR text and its cleanup/format pass degrades it (the transcriber already keeps a raw form — the prototype source was `…​.raw.md`); spaCy is a marginal dep there (already whisper/torch-heavy) but a new dependency class in coursekit (deliberately light: pydantic/openai + stdlib, no video stack); and the two tools already meet only through `.vtconfig/` files, so `voice.md` joins `domain.md`/`knowledge.json` (transcriber writes, coursekit reads via `courseconfig`, neither imports the other). Application — inject `voice.md` into generation + consolidation prompts (`domain_preface`-style) and score prose against it — is unambiguously coursekit. **Fallback nuance (mirrors the concept map's text-fallback):** coursekit needs a lighter text-fallback extractor for the document-ingest path (readings/PDF courses, no transcriber, no spoken audio) — there "voice" is written-source register and the cleanup-destroys-it concern doesn't apply; so the spaCy MECHANISM may be shared code while the rich raw-transcript invocation is the transcriber's. Voice is also a property of the PERSON (stable across their courses), a further argument for capturing it once at the source. **Prototype exists** at `~/spacy-test/app2.py` (register/voice feature profiler: cleans transcripts, foregrounds topic-independent interpersonal markers — hedges/discourse-markers/2nd-person/contractions — places each on a conversational↔academic continuum instead of noun-dominated raw sums, pulls real example utterances for the LM, takes transcript paths + aggregates). Two calibration caveats found: the reference anchors are dialogue-calibrated so a monologue lecture under-reads, and ASR under-punctuates questions — recalibrate anchors to a spoken-lecture baseline before trusting the composite index. **Before productionizing: re-read `~/video_transcription` to confirm the transcriber's exact raw-vs-cleanup stages** (per the facticity rule — claims about the transcriber are read, not reconstructed). **BUILT 2026-08-02 — both halves.** Transcriber side (read-confirmed: `vt_transcribe` writes `.segments.json`+`.raw.md`, `format` is the register-flattening cleanup): `vt_register.py` (deterministic profiler, spaCy lazy) + `vt_voice.py` (the `voice` step — aggregates raw transcripts course-wide, writes `.vtconfig/voice-features.json` + a seed-once `voice.md`) + `prompts/voice/default.md`; default model `en_core_web_trf` (torch already present there). coursekit side: `CourseConfig.voice` + `voice_preface` (tone-only, subordinate to correctness, with an explicit precision guard for stems/answers), injected into the PROSE generators (page + quiz `context.py`) and both fix loops — but NOT the critics (voice isn't a correctness axis). A real `voice.md` was generated on ARST260 and reads authentically. **Deliberately excluded / still open:** concept-map consolidation is NOT voiced (structured planning artifact; clarity as a checklist wins, and the enduring understanding gets re-voiced when the page is written); a **voice-match evaluator** (score generated prose against the profile — the fourth evaluator, parallel to the pedagogy rubric) is unbuilt; coursekit's **text-fallback extractor** for the no-transcriber document path is unbuilt (the spaCy mechanism could be shared); anchors are still dialogue-calibrated (recalibrate to a spoken-lecture baseline). This also **unblocks the concept-map-parametrized idiom titles** below — the idiom artifact now exists.
- **Concept-map-parametrized section titles in the professor's idiom** `[generate/profiles]` (user, 2026-07-31) — PARKED, blocked on the voice/tone profile above. Assessed 2026-07-31: the request is to title each section NOT with the raw concept name but by passing the concept (name/gist/level, from the map) as a PARAMETER to the model to get "an equivalent title in the idiom of the professor." **We have one half, not the other.** Present: the concept map is already loaded into page generation (`load_for_unit`), so the concept is available to parametrize a title. Missing — and it is the defining ingredient: any representation of the professor's titling *idiom* (register, voice). No voice artifact exists; the only "style" in the repo is visual design tokens (themes), which is orthogonal. Without a captured idiom the model would echo the concept name or invent a generic title — exactly what the user said NOT to do — and inferring the idiom ad hoc mid-generation reintroduces the attention crowding that produces the `》`/`>>` heading junk in the first place. **Right sequencing:** build the voice/tone profile as an editable artifact first (the analog of the concept map / `domain.md`), THEN (c) is a clean title step parametrized by (concept, idiom). This is also the deeper fix for the heading-creep symptom — a titling pass fed the concept + idiom, rather than free-authoring headings deep in a crowded generation.
- **Pluggable pedagogic strategies** `[profiles]` — per course/professor; anti-staleness (one fixed style goes stale); today's hook/key-idea/consolidation fix is one _default_ strategy, not the only law.
- **Document the config convention** `[profiles]` — `.yaml` = settings the code reads; `.md` = prose the model reads (`domain.md`, prompt overrides, future pedagogy profile). Put it in `docs/configuration.md`.
- **Course-level evaluator** `[deliver]` — spacing/spaced-retrieval, pacing, sequencing, cross-week coverage. (Spacing is a schedule property, so it belongs here, not on a single page.)
- **Prerequisite gap analysis** `[analyze]` (user, 2026-07-30) — the payoff that makes concept `prerequisites` worth getting right. Principle the user pinned: **prerequisites are a property of the CONCEPT (its intrinsic dependencies), not the COURSE (its coverage)** — deriving them from "what earlier weeks taught" mirrors coverage and makes gap-finding impossible by construction. So the concept map stores coverage-INDEPENDENT prereqs (the consolidation prompt now enforces non-self-referential, true-dependency prereqs); a later cross-week pass matches each concept's prereqs against the actual concept graph across weeks + a small "assumed incoming background" set, and a prereq satisfied by NEITHER is a **content gap** (the course assumes something it never taught and the student doesn't bring). This is the cross-week analysis the `analyze` phase was designed to grow into. Note: prereqs do NOT feed page generation today (only name/gist/key_material/enduring_understanding do), so weak prereqs are latent, not harmful, until this lands. Also fold in `teaches_toward` for sequencing.
- **Front-end / form factor** `[deliver]` (2026-07-30) — the CLI is a prototype; a friendly GUI is the highest-leverage step for reaching beyond early adopters, and eventually a packaged desktop app with Ollama bundled (so no manual model setup). The decisive usability track. Product rationale lives in the offline product doc.
- **Visual / design-forward track** `[deliver/visual]` (2026-07-30) — a coherent direction, not one feature: design-forward, pedagogically-grounded visual output + a visual authoring surface is the product's most authentic differentiation (the pedagogy↔design CLT/UDL layer already exists; the concept map already IS a graph). The front-end and "design for engagement" are the SAME build. Clusters parked items into one thesis: generated diagrams on pages (images shipped; diagrams next); the visual/design-legibility evaluator (a vision model that SEES the page); a concept-map-as-node-graph editor (Novak form, replaces YAML authoring); live page preview + theme gallery; evaluation as a course-health dashboard; embedded interactive sketches (p5.js iframes). Product rationale + a visual-first mockup live offline.
- **Canvas extension / LTI** `[deliver]` — plausibility of surfacing the tool inside Canvas. CLARIFY first: _which_ chatbot — coursekit's own generation surfaced interactively, or a separate project?
- **RAM pre-flight false positive** `[deliver]` (user, 2026-07-31) — `hardware.check_fit` warns "model ~14.7 GB but budget ~9.2 GB (available × 0.7), may fail to load" when loading right after ejecting another model; the model loads fine (available RAM not yet reclaimed; LM Studio evicts-then-loads + macOS swap). Advisory only (prints, never blocks). Fix: when a _different_ model is loaded, add its footprint back to `available` before the budget check (it will be evicted), or add a total-RAM factor (transcriber uses available-0.7 / total-0.35). Low priority — it's a cosmetic false alarm.
- **One-off:** regenerate the one thin page (a Functions week scored low on delivery — under-generated).

### Carried over from the increment history (reconciled 2026-07-30)

Increments 1–10 are complete in code; these are the live remnants, so nothing real is lost. The offline plan file is now history.

- **Non-coding readings → page end-to-end** `[generate]` — Increment 8's acceptance gate: a real readings/PDF course (no transcripts) through `ingest → generate --pages`, confirming a sensible non-coding page. A user model run.
- **Canvas API push** `[deliver]` — Increment 3/6 Phase 3: `POST /api/v1/courses/:id/pages` with the rendered HTML; defaults to dry-run, gated on a running Canvas + token. File emitters stay first-class regardless.
- **Quiz deterministic verifier** `[evaluate]` — Increment 10 steps 4–5: execute/compute decidable questions (loop trace, arithmetic) in a sandbox instead of trusting the LLM. Deliberately deprioritised — the critic's 100% wrong-answer recall weakened the rationale; revisit only if a decidable-slice failure resurfaces.

## Shipped

A running changelog — one line per completed feature, newest first, with its commit(s).
The sections above stay forward-looking; deep detail lives in the offline log + git.
(Increments 1–9 — GIFT/QTI/CC emitters, `courseconfig`, the quizbot→coursekit rename, the design system, document ingest — predate this changelog; see `architecture.md`'s "Order of operations" and git history.)

- 2026-08-02 · **Voice on quizzes scoped to feedback only** — after voice went live, a full-course run showed quiz flags rise (~2% → ~6%) while pages improved: a hedged register bleeds into stems/options ("asks why, but the options are syntax"). So the quiz generator + quiz fixer now use a narrower `quiz_voice_preface` (voice in feedback/explanations only; stems and options stay literal); pages keep the full voice. _(pending commit)_
- 2026-08-02 · **Generate's auto-review archives to the run-store** — the cold read after `generate` now snapshots to `<course>/evals/<timestamp>/` + `log.jsonl`, same as `evaluate`; `_review_quizzes`/`_review_pages` return `(review, metrics)`. `b298def`
- 2026-08-02 · **Voice/tone profile — both halves** — transcriber `voice` step (`vt_register` + `vt_voice` + `prompts/voice`, `en_core_web_trf`) writes `.vtconfig/voice.md` from the raw transcripts; coursekit reads it (`CourseConfig.voice` + `voice_preface`) and injects it into the page + quiz generators and both fix loops — tone only, precision-guarded, and never into the critics. Committed (with the finalize empty-section guard + the task.md heading-order/hook fixes). _(quiz application later narrowed to feedback-only, above)_
- 2026-07-31 · **Equation renders plainly, not as literal source** — a bare `$…=…$` span (an equation the model delimited, no LaTeX command inside) was shipping with its dollar signs intact; a deterministic renderer pass now strips the delimiters, gated on a relation inside a math-only body so prose dollars (`$5 and $10`, a `$5 - $10` range) stay untouched, and running after the LaTeX passes so unresolved `\command` spans are still preserved for MathJax. `7569632`
- 2026-07-31 · **Versioned evaluation run-store** — each `evaluate` snapshots the run immutably to `<course>/evals/<timestamp>/` (review files + `summary.json` with the coursekit commit, model, and counts) and appends `evals/log.jsonl`, so quality is a readable trend, never an overwritten file; the top-level `*-review.md` stay as latest copies. `883e34f`
- 2026-07-31 · **Images on pages** — inline `ImageBlock` + `add_image`: the model places a `ref`/alt slot (never a URL), the instructor supplies the file via supplements, the renderer resolves it or shows a placeholder. `ecb26b3`
- 2026-07-31 · **Targeted fix loop (quiz + page)** — regenerate each flagged item in place + verify; from-review default (`parse_review`), a progress heartbeat, and the emit robustness guard (skip an out-of-sync week rather than KeyError). `024cc32`
- 2026-07-31 · **Canvas whole-course `.imscc` import — verified in real Canvas** (manual acceptance gate; pages + quizzes as week modules; no code commit).
- 2026-07-30 · **Concept map + the `analyze` phase** — per-week concept map (schema, `consolidate`, the `analyze` verb) from `knowledge.json`, with a text-fallback extractor for non-transcriber courses; wired into page generation (an un-skippable teaching checklist) + concept-delivery scoring. `838a12a`, `b755318`
- 2026-07-30 · **Content-relative page length + teaching structure** — task.md sizes the page to the concept count; one-idea-per-paragraph; each concept its own section. `e1ac724`
- 2026-07-29 · **Page-build hardening** — `add_code` `text` alias, columns multi-line code rendering, longer-content handling, finalize-stop dispatch (fixes the `--detail full` runaway). `0766f86`
- 2026-07-29 · **Concept-delivery evaluation** + `evaluate --all` / `--no-review`. `509d4ba`, `31d20ba`
- 2026-07-28 · **Page pedagogy rubric** — evaluate covers pages + pedagogy, critic domain-awareness, and the loop-back prompt fix (course pedagogy 9.25 → 13.5/15). `06ab78b`
- 2026-07-28 · **Page facticity review** + section scoring. `f55677a`
- 2026-07-27–28 · **Quiz evaluator** — cold-read critic, synthetic calibration, multi-read union, scorecards, reads param. `fe87d05`, `775ebe9`, `9f9a224`, `29840e5`, `b54c400`, `a770f99`
- 2026-07-31 · **Retrieval-foldout finalize gate** — `page.validate_final` requires a `details` block so foldouts stop being dropped (marked provisional; see the parking-lot note). `a58ed2c`
- 2026-07-31 · **Flexible quiz question count** — the quiz now reads the week's concept map and makes one group per teaching concept + one for the enduring understanding (variants drawn from each concept's knowledge components), replacing the hardcoded five; the model suggests the count from the map (or from the material when no map). Overrides at two levels: `questions:` on a week's `.vtconfig/concepts/week-N.yaml` (per-week, wins) and `questions:` in `.vtconfig/quiz.yaml` (per-course). Shared `concept_map.load_for_unit` used by both generators; `quiz/context._shape_directive`; task.md generalized. `ad54acc`

## Note

`agent/todo.md` predates this file; its live items should migrate here (into the parking lot or a theme) so there is one forward view, not two.
