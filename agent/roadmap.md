# coursekit roadmap

The forward-looking view.
For the detailed record — measurements, dated decisions, per-course results — see the working log (kept offline).
This file is the map; the log is the diary.

## Vision

> coursekit turns a course's own material into Canvas-ready artifacts that are **correct, well-taught, and reviewable** — locally, and measurably.

Every decision checks against that line: does it make artifacts more correct, better-taught, more reviewable, or the tool more usable to a real instructor?
If not, it goes to the parking lot.

## How we work (the cadence)

1. **One active focus at a time.** A single _current milestone_ (below). Everything else waits.
2. **Parking lot.** A new idea gets written down immediately so it is never lost — and is **not acted on** until a regroup. Ideas are welcome; acting on them mid-focus is not.
3. **Every milestone has a "done when."** A concrete, ideally measurable acceptance (e.g. "recall > X", "5/5 discrimination", "the review runs from one command"). No open-ended work.
4. **Regroup at milestones.** When the current thing lands: review the parking lot, re-prioritise, pick the next focus. Only then.

## Current focus

> **Just shipped (2026-08-06): the FUNCTION axis + auto-router.** `coursekit generate --pages --function teaching|glossary|overview`; the teaching generator (monolithic vs decompose) is chosen by a deterministic multi-signal router (length + concept count + largest span, tunable in page.yaml) with a `--generator auto|monolithic|decompose` override; the monolithic `--detail` is gone (decompose's already was). All functions write under `pages/`. **One partial (parked):** `emit` sees every function, but `evaluate`/`fix` still discover only the per-week teaching page, not the `-glossary`/`-overview` siblings — parked as "evaluate/fix should cover all page functions". **Next-focus candidates now** (H1 in the outline): the **depth-planner** (`PAGE-4`), the cheap **review/orientation pages** (`PAGE-3`), the **glossary formatting fix** (`PAGE-1`), or lay the **component foundation** (`COMP-1`) before the content-type expansion. Regroup and pick one.

The forward view is organized by **capability area** with semantic IDs — see **Structure & numbering** and the **Roadmap outline** below; the **Detail** section keeps each item's dated history, tagged with the same ID.

## Structure & numbering

The forward view is organized by **capability area**, with a stable semantic ID on every item.

- **ID = `AREA-N[.M]`** — `AREA` is the capability; `N` is **do-order within that area** (1 = do-first; a sequence, not a priority score); `.M` a sub-step. IDs are stable; horizon/status move.
- **Horizon** (when, across areas): `H0` now · `H1` next · `H2` later · `H3` someday/research.
- **Status:** ✅ shipped · 🔶 partial · ⬜ open · ✨ proposed-gap · 🧭 new-capability (2026-08-06 expansion).
- The **outline below** is the structured view; the **Detail — dated history** section keeps each item's full record, tagged with the same ID. New items (✨/🧭) live only in the outline until worked.

**Areas** (each: one-line standing):

- **`COMP`** components & composition — the shared block vocabulary; seed device↔function table in `docs/design.md`, otherwise unbuilt.
- **`PAGE`** page generation — mature: `page.json` IR, monolithic + decompose, function axis + auto-router, boundary segmentation.
- **`ASMT`** assessment & activities — quizzes shipped (6 types, flexible count); assignments/discussions/rubrics/etc. new.
- **`STRC`** course structure & grading — modules, weighting, points, mastery paths; unbuilt (Canvas gradebook/config objects).
- **`EVAL`** evaluation & quality — 3 critics built + calibrated; deepenings + *upstream* (generation-deciding) evaluation open.
- **`PROF`** profiles & adaptation — `domain.md` + voice profile shipped; the rest parked.
- **`CRSE`** course-scope & advisory — cross-week reasoning + gap advisory; unbuilt (the concept graph is the substrate).
- **`SHIP`** LMS delivery — QTI/HTML/CC/whole-course `.imscc` shipped (Canvas-verified); structure-emit + API open.
- **`FLOW`** inputs, ingest & modes — transcript + document ingest + `analyze` shipped; convergence + new modes open.
- **`RICH`** rich content & embeds — instructor images shipped; diagrams, provider embeds, 3D open.
- **`UX`** interface & human-in-the-loop — none built; the CLI is the prototype. The decisive usability track.
- **`SVC`** hosted-service tier — technical seams only in-repo; the product/market framing lives OFFLINE.
- **`OPS`** tooling & housekeeping.

## Roadmap outline (by area · do-first → do-last)

**COMP** *(foundation — build before multiplying content types)*
- `COMP-1 · H1 · ✅` Component catalog: every block/device + the pedagogic function it serves — `coursekit/generate/catalog.py` (a registry, drift-guarded against `page._KINDS`).
- `COMP-4 · H1 · ✅` `docs/components.md` — generated from the catalog, freshness-tested.
- `COMP-2 · H2 · ✅` One shared block vocabulary all content types compose from — `coursekit/generate/blocks.py` (the content-type-neutral import point) + catalog governance (`CONTENT_TYPES`, `components_for`/`allows`/`unknown_components`). The class DEFINITIONS stay in the page IR behind the re-export until content type #2 lands (extract-at-second-consumer). ⇐ COMP-1
- `COMP-3 · H2 · ✅` Pedagogically-driven composition (v1, prompt-injection) — `catalog.render_palette` renders the component guidance from the catalog, injected as `{palette}` into `system.md` (monolithic, full page palette) and `concept_section.md` (decompose, a section-scoped subset), replacing the hand-written device lists that drifted across the prompts. Both generators now compose by function from ONE source. ⇐ COMP-2. Under the current single default pedagogy; PROF-3 later parameterizes the strategy. **Acceptance is a real-model A/B (yours):** regenerate a week before/after, scored by the three evaluators — should be wash-or-better.

**PAGE**
- `PAGE-1 · H1 · ⬜` Glossary formatting bug (empty "· Key Terms Recap" kicker). ↓
- `PAGE-2 · H1 · ⬜` `evaluate`/`fix` cover all page functions (rglob `pages/**/page.json`). ↓
- `PAGE-3 · H1 · ✨` Cheap orientation/**review** pages (week review ≈ free from concept maps).
- `PAGE-4 · H2 · ⬜` Depth-planner — a genuinely shorter *teaching* page from a whole-week view. ↓
- `PAGE-5 · H2 · ⬜` Glossary slidable size by importance. ↓ · `PAGE-6 · H2 · ⬜` Glossary vocabulary typology. ↓
- `PAGE-7 · H2 · 🔶` Temporal curation — fix loop settles CLEAN corrections (✓ measured 2026-08-07); residual is self-contradictory SOURCE → UX-1 / transcriber, not a downstream lever. ↓
- `PAGE-8 · H2 · ⬜` Per-video / sub-unit decomposition (bound to measured capacity). ↓ ⇐ SHIP-2
- `PAGE-9 · H3 · ⬜` Non-linear / revisiting lecture representation. ↓ ⇐ PROF-3
- `PAGE-10 · H3 · ⬜` Page-type taxonomy as `(scope × role × source)`. ↓
- `PAGE-13 · H2 · ✨` **The flexible-generation request space — SCOPE × ARTIFACT × GROUNDING** (user, 2026-08-07; generalizes PAGE-10 + ASMT-17 across pages AND quizzes). A week's knowledge is used many ways — a technical quiz vs an open aesthetic question; a technical-review summary vs a whole-week page; a glossary; an artist list or bibliography NOT in the lecture. Three orthogonal axes: **(1) SCOPE** — whole week · one SOURCE (ASMT-17 ✅) · a set of CONCEPTS (the concept map + segmentation already make this addressable) · a TYPE-slice (technical vs aesthetic/interpretive — a classification layer we lack, ⇐ EVAL-3 / vocabulary typology). **Key finding:** a FLAT consolidated `week-N.md` is NOT addressable enough for the type/concept slices — consolidation gives GROUNDING; the CONCEPT MAP (+ source headers + a type tag) is the INDEX that makes "quiz on Y, page on X" possible. So FLOW-2 consolidation must PRESERVE source structure, not flatten it. **(2) ARTIFACT** — teaching page · summary/review · quiz · glossary · overview · bibliography · exemplar/artist list (the page FUNCTION axis + assessment types). **(3) GROUNDING** — from-source (default) · instructor-SUPPLIED (a bibliography/artist list "not in the lecture" is CURATED via supplements — grounded in the instructor, never fabricated) · model-SUGGESTED-and-flagged (an opt-in "draft me further reading" mode, marked UNVERIFIED — scores 0 on EVAL-15 by design). **coursekit must NEVER silently fabricate references/citations** (the no-URL/no-invention guard). Ties PAGE-10, ASMT-17, EVAL-3, EVAL-15, and supplements.
  **Deepened 2026-08-07 (user — the axes are MEASURES, not just knobs; the epistemic model).** Each axis is a *measured relationship*, and each maps to an evaluator coursekit already has or is building: **SCOPE** = the extent of the field the material implies → measure = **coverage** of the concept map (the map is a *sample* of a larger field; how much of the space is present). **ARTIFACT** = how well an artifact illuminates the scope → measure = **concept-delivery** (does it teach the concepts, PAGE concept_delivery) crossed with **groundedness** (is it evidenced by the source, EVAL-15). **GROUNDING** = the interface between FACULTY knowledge and LLM knowledge, both fallible — LLM knowledge *measured* (never assumed bad), faculty knowledge *respected but not infallible* → measure = EVAL-15 groundedness + surfacing where model and material *disagree* (the "faculty isn't infallible" half). So the three axes are the three checks the evaluator arc has been converging on: coverage · concept-delivery · groundedness. **Epistemic models it reflects** (none is a perfect fit; the synthesis is the contribution): Toulmin's argument model (claim/grounds/warrant — the artifact↔grounding "is it evidenced?" relation); backward design / UbD (scope → evidence → learning — the scope↔artifact relation, already coursekit's frame via enduring understandings); **social epistemology / reliabilism / the epistemology of testimony** (the grounding axis IS this — two knowledge *sources*, neither infallible, trust *calibrated* rather than assumed — the deepest fit); **RAG faithfulness/attribution** (the technical instantiation of the grounding measure = EVAL-15); and HCI's **calibrated trust / appropriate reliance / human-AI complementarity** (the goal is not the model replacing faculty judgment but a faculty member *seeing* how the two knowledges relate). **The distinctive move — coursekit's actual thesis:** treat the faculty↔LLM epistemic relationship as a *first-class, measured, represented* object for a "cognizant faculty" — not "is the AI right" (facticity alone) but "how do the two knowledges relate, and can the instructor SEE that relationship." That reframes coursekit from a generator-with-a-checker into an *epistemic instrument*.
- `PAGE-11 · H2 · 🧭` Inline placement of non-generated assets (generalize the image `ref` slot to sketches/embeds/decks). ⇐ RICH-4, COMP-2
- `PAGE-12 · H2 · ✨` Semantically split a long page into a MULTI-PAGE sequence (user, 2026-08-07 — decomposed pages come out very long). Decide "too long" by **cognitive load**, not char count, and break at CLT-sound seams (concept boundaries — segmentation already marks them — or per-video), with a short **retrieval checkpoint between segments** (a few recall questions / a mini-quiz) to consolidate AND gauge student progress. Open strategies: split by concept, by video, or by a load estimate; the retrieval check is the pedagogically-motivated seam, not arbitrary length. Sharpens PAGE-8 (unit-of-decomposition) toward *reader-experience* pagination; pulls in ASMT (the inter-page checks), EVAL-2 (development-adequacy / load), and CRSE (spacing across the sequence). ⇐ PAGE-8, EVAL-2
- `PAGE · note` Provisional retrieval-foldout finalize gate — partially resolved (page-type-aware). ↓

**ASMT**
- `ASMT-1 · H1 · ⬜` Stop minting an MC group for the enduring understanding. ↓
- `ASMT-2 · H1 · ⬜` Quiz introduction rework. ↓
- `ASMT-3 · H2 · ⬜` Enduring-understanding instrument (performance task + rubric, human-graded). ↓ ⇐ ASMT-1, ASMT-7
- `ASMT-4 · H2 · 🧭` Assignments (brief + submission type + instructions).
- `ASMT-5 · H2 · 🧭` Discussions (prompt + facilitation guidance + starters). ⇐ COMP-2
- `ASMT-6 · H2 · 🧭` Peer-review workflow + guidance (setting on ASMT-4/5 + review rubric).
- `ASMT-7 · H2 · 🧭` Rubric generator (reusable, attaches to assignments/discussions/EU tasks). ⇐ COMP-2
- `ASMT-8 · H2 · 🧭` Group projects (deliverable + roles + shared rubric). ⇐ STRC-2
- `ASMT-9 · H2 · 🧭` Social / community spaces ("cyber café" intros, water-cooler).
- `ASMT-10 · H2 · 🧭` Longer-form / open-response question types (feeds ASMT-3).
- `ASMT-11 · H2 · ✨` Quiz feedback/explanation quality evaluated. · `ASMT-12 · H2 · ✨` Difficulty/Bloom distribution.
- `ASMT-13 · H3 · 🧭` Surveys / ungraded feedback. · `ASMT-14 · H3 · 🧭` Shared item/question banks.
- `ASMT-15 · H3 · ⬜` `synthesize.py` for generation. ↓ · `ASMT-16 · H3 · ⬜` Visual/image-bearing question types. ↓
- `ASMT-17 · H2 · ✅ (v1)` **Targeted quizzes — scope a quiz to ONE element, not the whole week** (user, 2026-08-07). **SHIPPED v1 2026-08-07:** `coursekit generate <course> --quizzes --source "<doc>"` — `generate/quiz/targeted.py` extracts one document (pdf/pptx/docx/odt/md/txt via `ingest.extract`), gives it a distinct slug (`targeted_slug` → `week-<n>-<doc>`, so it never collides with the week quiz), persists the text as `quizzes/<slug>/source.md`, and runs the ordinary quiz generator (domain.md/voice/quiz.yaml resolve from the source's `.vtconfig`; works with no concept map via the model-suggested count). Deferred: (c) element-scoped concept extraction, and open-response for interpretive readings (⇐ ASMT-3, EVAL-3). Generate a quiz from a specific source — a single reading, one slide deck, a chosen document — instead of the assembled week text. **Feasibility: low–moderate** — the generation engine is already material-agnostic (quiz gen is text + optional concept map → `bank.json`; it doesn't care whether the text is a whole week or one reading), so the work is SCOPING + NAMING, not new generation: (a) an INPUT selector (point at one element / treat a doc as its own unit — a folder-per-week course already keeps each reading/deck as a separate file); (b) an OUTPUT convention for MULTIPLE banks per week (a distinct slug/dir per targeted quiz — `quizzes/week-3-reading-barrett/…` — + emit discovery of them), which is the EXACT pattern the page FUNCTION axis just established (pick source → distinct output → emit-uniform); (c) concept GROUNDING from the element itself (the text-fallback extractor over just that reading, not the week map). Nuance: interpretive readings suit open-response over MC (⇐ EVAL-3, ASMT-3). Directly motivates the per-reading "reading quiz" real course outlines already call for. The quiz analog of the page unit-of-decomposition question (⇐ PAGE-8).

**STRC** *(Canvas structural/gradebook objects — emitter/config work)*
- `STRC-1 · H2 · 🧭` Modules as first-class output (assemble an ordered module from items).
- `STRC-2 · H2 · 🧭` Assignment groups + weighting + group sets.
- `STRC-3 · H2 · 🧭` Points & point-weighting across items.
- `STRC-6 · H2 · 🧭` Prerequisite / requirement gating. ⇐ CRSE-1
- `STRC-4 · H3 · 🧭` Drop rules / late / extra-credit policies.
- `STRC-5 · H3 · 🧭` Mastery paths / conditional release.
- `STRC-7 · H3 · 🧭` Learning outcomes + rubric alignment (accreditation).

**EVAL**
- `EVAL-1 · H1 · ⬜` Concept-delivery v2 (pin extraction to material). ↓
- `EVAL-2 · H2 · ⬜` Development-adequacy / coherence dimension (over-compression). ↓
- `EVAL-3 · H2 · ⬜` Visual pass of the rendered page (= RICH-3). ↓
- `EVAL-4 · H2 · ⬜` Quality vs presence (good, not merely present; near-miss facts). ↓
- `EVAL-5 · H2 · ⬜` Voice-match evaluator (4th critic). ⇐ voice ✅
- `EVAL-6 · H2 · ⬜` Multi-model reads. ↓ · `EVAL-7 · H2 · ⬜` Correctness vs style (note, not flag). ↓
- `EVAL-8 · H2 · ⬜` Pedagogy rubric — more dimensions. ↓ · `EVAL-9 · H2 · ✨` Accessibility audit as output.
- `EVAL-10 · H2 · 🧭` Generation-decision evaluation — content-type↔pedagogy fit to *drive what to generate*. ⇐ PROF-3, COMP-3
- `EVAL-11 · H2 · 🧭` Class-level engagement + form evaluation (whole class, not just a week). ⇐ CRSE-2
- `EVAL-12 · H2 · 🧭` Rich content is framed & evaluated (anti-shiny-toys). ⇐ RICH-*
- `EVAL-13 · H3 · ⬜` Retire hand-authored synthetic set. ↓ · `EVAL-14 · H3 · ⬜` Quiz deterministic verifier. ↓
- `EVAL-15 · H2 · ✨` **Source-groundedness — measure & store how much generated content is FROM THE SOURCE vs the model's prior knowledge** (user, 2026-08-07). Surfaced when a targeted quiz on a <10-word ISO slide produced a full, correct quiz — the model clearly leaned on its OWN photography knowledge, not the slide. A NEW evaluation dimension, orthogonal to the three we have: facticity asks "is it CORRECT?", never "is it FROM THE SOURCE?" — a model-knowledge question can be perfectly correct AND ungrounded, which quietly breaks coursekit's core thesis (a course's OWN material; local-first, faculty-owned content). **Measure + store:** a groundedness score per artifact (and per element) = the fraction of generated content ATTRIBUTABLE to the source, archived to the run-store like the other metrics, surfaced as a WARNING when low ("this quiz is mostly the model's knowledge, not your 10-word slide — the source is too thin to ground it"). **How (tractable → deep):** (a) a **groundedness critic** — per element, "is this supported by the source? quote the span, or say NOT-IN-SOURCE" → score + evidence, reusing the critic + run-store pattern; (b) lexical/semantic **overlap** of an element's key claims against the source (cheap, crude, paraphrase-blind); (c) **ablation** — generate with vs without the source; near-identical output ⇒ the source added little (expensive, 2× gen). Start with (a). **Knowledge-type nuance (⇐ EVAL-3):** "grounded" differs by type — an interpretive reading isn't quoted verbatim, so groundedness there means "consistent with / prompted by" the source, not "lifted from" it. Ties to the no-invention guard (stops fabricated URLs/examples, but NOT ungrounded *concepts*), the thin-source problem (ASMT-17), the domain profile (general knowledge vs the course's framing), and the product's local-first differentiation.

**PROF**
- `PROF-1 · H1 · ⬜` Document the config convention. ↓ · `PROF-2 · H1 · ⬜` Idiom titles (unblocked). ↓
- `PROF-3 · H2 · ⬜` Pluggable pedagogy strategies. ↓ *(generalizes COMP-3 into selectable strategies; enables EVAL-10, PAGE-9)*
- `PROF-4 · H2 · ⬜` Composable domain bases. ↓ · `PROF-5 · H2 · ⬜` Domain-suggester. ↓ ⇐ PROF-4
- `PROF-6 · H2 · 🔶` Voice remnants (text-fallback extractor; recalibrate anchors). ↓

**CRSE**
- `CRSE-1 · H2 · ⬜` Prerequisite gap analysis. ↓
- `CRSE-2 · H2 · ⬜` Course-level evaluator (spacing/pacing/sequencing). ↓
- `CRSE-4 · H2 · 🧭` Domain/curriculum advisory — detect → suggest → plug gaps (coursekit as advisor). ⇐ CRSE-1, PROF-4
- `CRSE-3 · H3 · ✨` Cross-week consistency (terminology/example drift).

**SHIP**
- `SHIP-1 · H2 · ⬜` Bundle instructor files into `.imscc` + manifest link↔object. ↓
- `SHIP-2 · H2 · ⬜` Video + timestamp references (survive imscc). ↓ ⇐ SHIP-1, PAGE-8
- `SHIP-3 · H2 · ✨` New Quizzes format (Canvas default; we're Classic-only).
- `SHIP-6 · H2 · 🧭` Emit the STRC objects (module_meta / weighting / grading rules / MasteryPaths). ⇐ STRC-*
- `SHIP-4 · H3 · ⬜` Canvas API push. ↓ · `SHIP-5 · H3 · ⬜` Canvas extension / LTI. ↓

**FLOW**
- `FLOW-1 · H1 · 🔶` Non-coding readings → page (acceptance gate; a model run). ↓
- `FLOW-2 · H2 · 🔶` Multi-source convergence. ↓ **v1 SHIPPED 2026-08-07 (text layer):** `ingest` now GROUPS a week's docs by their `week-N` ancestor directory (folder-per-week) and CONSOLIDATES them into one `week-N.md`, each source under a `## <source>` header so the week stays source-addressable (not a flat blob — PAGE-13); a duplicate week is a union, not an error; a root-level course outline is dropped from week content; single-source weeks unchanged. `ingest.plan_weeks` returns `[(week, [sources])]`. **v2 (open):** the KNOWLEDGE-layer aggregation the original item meant — per-source `knowledge.json` tagged by kind (video/reading/slides) unioned into one concept map (for video+reading mixed courses; ARPH201 didn't need it, no transcriber).
- `FLOW-3 · H2 · ✨` Update-in-place / refresh (README vision; regen overwrites, never diffs).
- `FLOW-5 · H2 · ✨` Audit an *imported* course (evaluate only reads coursekit-generated content today).
- `FLOW-4 · H3 · ✨` Outline → course (a different generation mode).
- `FLOW-6 · H2 · ✨` **Source-quality evaluation belongs UPSTREAM (videotranscriber), not here.** coursekit's facticity critic surfaced a defect it structurally cannot fix: a transcript mislabeled a WRONG `setRed()` explanation as "The Correct Implementation", contradicting its own later correct code — user confirmed against the source video that the *video is fine; the transcriber's prose is wrong*. No downstream generation/fix lever can resolve a self-contradictory source. So the transcriber needs its OWN **transcript-quality evaluation** — catch internal contradictions, mislabeled "correct" claims, and prose that disagrees with the code it shows — analogous to coursekit's output evaluators but on the *source*. This lives in the videotranscriber repo (the two tools stay separate, meeting only through files); tracked there, pointer here. The user also wants the **transcriber to adopt this roadmap's structure** (capability areas + semantic IDs) — a separate deliverable in that repo. Ties to PAGE-7 (teaching-is-temporal) and EVAL-4 (critic false-flags on self-contradictory material).

- `FLOW-7 · H2 · 🔶` **Declared course-structure IR — stop inferring structure from filenames (user, 2026-08-18).** **v1 (Phase 1) SHIPPED 2026-08-18 (reader + `Unit.week_num` + manifest-driven discovery, all additive/offline):** new `coursekit/coursestructure.py` (`CourseStructure` reads `context.yaml`'s `weeks` as the declared IR — typed `sources:[{path,title,kind,role}]` + optional `doc:`, normalizing the transcriber's untyped `videos:` into kind=video; `has_declared_structure()` fires only on coursekit's own `doc`/`sources` keys so existing courses are untouched). `Unit` gained a carried `week_num` (the 6 `week_key(unit.week_slug)` round-trip sites now read it). `discover.find_units` builds units from the declared weeks when present (a week doc can be named ANYTHING, not just `week-N.md`), else today's `week-*.md` glob. **Phases 2-4 (open):** the proposer/bootstrap that DRAFTS the manifest + surfaces unassigned files (= [[UX-2]]; kills the silent-drop issue) — needs the coursekit-writes-shared-`context.yaml` decision settled with `vt_init`; then retire the inferrers (`ingest.plan_weeks`/emit `week_key(dir.name)` → declared identity); then the optional model-assisted proposer. Full design: plan file Increment 12. Root diagnosis: coursekit *infers* structure by convention (`find_units` globs `**/week-*.md`; `_make_unit` derives the week from `week_key(stem)`; `context.yaml` only DECORATES with titles/modules) — and convention-over-configuration is brittle precisely because naming won't be consistent across faculty. Every regex we add (`week-N`, the ancestor `_WEEK_DIR_RE`, `week_key`) is a guess at an unbounded input space. **Fix = read structure from an explicit manifest, not the tree.** A `.vtconfig/structure.yaml` (or a promotion of the partial `context.yaml`) maps ARBITRARY paths → weeks → roles: `weeks: [{id, title, sources: [{path, role: reading|slides|outline|video}]}]`. `find_units` reads it and yields units directly — the manifest IS discovery; `week_key`/the ancestor regex survive ONLY inside a proposer. **Key design principle — relocate the guessing, don't eliminate it (two layers):** (1) the declared IR is the source of truth the program reads (no runtime inference); (2) a ONE-TIME proposer drafts it from whatever the faculty has (folders/filenames/`context.yaml`/model-read of contents), faculty confirm/edit once. The heuristics become a *suggestion engine at onboarding*, not a load-bearing parser — a wrong guess is a one-line edit in a visible file, not an unfixable in-code regex. **Kills issue-3 (silent drops) for free:** an unmapped file is visibly UNASSIGNED, surfaced at onboarding ("assign or ignore?"), never silently discarded. **It's the structural spine FLOW-2/PAGE-13/ASMT-targeting already want:** `role` tagging = FLOW-2's source-tags; targeting a source = selecting a manifest entry, not re-deriving a slug from a path. **Patterns:** manifest/explicit-descriptor over CoC (`pyproject.toml`/dbt); anti-corruption layer (stable internal model insulating generators from messy faculty layouts); same shape as the domain-profile (model suggests, `.vtconfig` is authoritative). **Migration without churn:** additive + back-compat — no `structure.yaml` → proposer synthesizes one from today's logic, so every `week-N.md` course keeps working; the regex is demoted to the bootstrap, never deleted (respects the don't-orphan-courses rule). The proposer half IS [[UX-2]] onboarding (bootstrap `.vtconfig`); share the shape with the transcriber's `vt_init` (⇐ SPINE-*). Underpins FLOW-2 v2, ASMT-17 targeting, PAGE-13. Park-and-plan (touches the whole input spine: discover · courseconfig · ingest · targeted).

**RICH**
- `RICH-1 · H1 · ⬜` Comparison-columns bug (bullets/code/indents). ↓
- `RICH-2 · H2 · ⬜` Generated diagrams (SVG/mermaid). ↓
- `RICH-3 · H2 · ⬜` Design-legibility evaluator (= EVAL-3).
- `RICH-4 · H2 · 🧭` Rich embeds — allowlisted iframes (Slides, Sheets, Miro, other providers) + placement.
- `RICH-5 · H3 · 🧭` 3D / interactive (three.js, live p5) via external-host iframe (Canvas strips JS).
- `RICH-6 · H3 · ⬜` Design-forward cluster — live preview, theme gallery, course-health dashboard. ↓ ⇐ UX-3

**UX**
- `UX-1 · H2 · ⬜` Block-level edit + re-run + vetting UI. ↓ ⇐ PAGE-7, EVAL-7
- `UX-2 · H2 · ✨` Onboarding / `init a course` — **coursekit never creates `.vtconfig` today; it's strictly read-only** (`courseconfig` finds+reads it, no write). The transcriber makes it (`vt_init`), so a coursekit-only course (readings/PDFs, no transcriber) needs a MANUAL `.vtconfig` (user hit this on ARPH201, 2026-08-07). Bootstrap it instead. **Design questions to settle:** (1) explicit `coursekit init <course>` vs **implicit first-run auto-create regardless of entry point** (any of analyze/generate/`--source`/emit bootstraps if none is found — the user's ask); (2) the **"where's the root?" ambiguity** — `.vtconfig` is precisely what *marks* the root, so auto-create must DECIDE one (sensibly: the directory the command points at, or the walk-up anchor for `--source`) rather than guess; (3) what to seed — a minimal `.vtconfig/` (a starter `domain.md`, optionally drafted by the domain-suggester `PROF-5`; `quiz.yaml`/`page.yaml` stubs). **Share the create logic with the transcriber's `vt_init` so both tools produce the SAME `.vtconfig` shape** (⇐ SPINE-1 / the `.vtconfig` contract SPINE-3) — a coursekit-side create must not fight the transcriber's. `courseconfig.load` already notes a "future write" seam.
- `UX-3 · H3 · ⬜` Front-end / packaged app. ↓ *(big by size, decisive by strategy)*
- `UX-4 · H3 · ⬜` Concept-map graph editor. ⇐ UX-3 · `UX-5 · H3 · ✨` Artifact versioning/rollback. · `UX-6 · H3 · ✨` Student-experience preview.

**SVC** *(product/market framing lives OFFLINE — technical seams only here)*
- `SVC-1 · H3 · 🧭` Provider neutrality already supports frontier/hosted models — keep the seam clean.
- `SVC-2 · H3 · 🧭` Client/university branding (extend theming to org identity).
- `SVC-3 · H3 · 🧭` Hosted specialized content / shared prompt + domain libraries.
- `SVC-4 · H3 · 🧭` Multi-tenancy / accounts / hosting — the real cost; flagged, not planned.

**OPS**
- `OPS-1 · H0 · ⬜` Commit the pending stack (*you commit*).
- `OPS-2 · H1 · ⬜` Regenerate the thin Functions page. ↓ · `OPS-3 · H1 · ⬜` Migrate `agent/todo.md` in.
- `OPS-4 · H2 · ⬜` RAM pre-flight false-positive fix. ↓ · `OPS-5 · H3 · ⬜` Graft dev-tool. ↓
- `OPS-6 · H2 · ✅` Fix loop mislabels infrastructure failures — DONE 2026-08-07. Both fix loops (`fix_one`, `fix_one_block`) now route a caught exception through a shared `_abort_if_model_error` that re-raises `pipeline.ModelLoadError` on a model-load/connection failure (reusing `_looks_like_model_error` / `_model_error_message`) so `_cmd_fix` aborts with a clear "model not loaded" message instead of grinding out per-item "could not fix"; a content-level exception still breaks just that one item. `render_outcomes` takes a `noun` (pages say "section(s)", not "question(s)"). This is the coursekit half of the error-boundary work — the shared spine module is `OPS-7`. (Surfaced 2026-08-07 — a `fix` run misreported a not-loaded model as 3 unfixable sections and sent us down a false PAGE-7 trail.)
- `OPS-7 · H2 · ✨` **Coherent model-boundary error handling — and how to share it across repos** (user, 2026-08-07). Error communication across the model↔program boundary (model-not-loaded, connection refused, timeout, empty/narrated reply, malformed tool call) is piecemeal in coursekit (`ModelLoadError` / `check_fit` in pipeline, the fix-loop's swallow-and-mislabel per OPS-6, decompose's error tally) and reinvented in the transcriber. Make it ONE small, dependency-light module in the shared SPINE — `coursekit/providers/`, which already owns the model boundary and was extracted from the transcriber: typed errors + one consistent way to surface them (abort clearly on an *infra* failure, never mislabel it as *content*). **Cross-repo reuse principle (not a monolith, not a premature shared repo):** shared code lives in the LIGHT spine (no heavy deps); the transcriber (heavy — mlx-whisper/torch) DEPENDS ON the spine, never the reverse; the two still meet at the FILE layer for data. This is the already-parked "transcriber → shared spine" consolidation (Increment-4 history) — the error module is one concrete, dependency-light piece to build *well in coursekit first* and have the transcriber adopt when the consolidation lands. **Subsumes OPS-6.**
- `OPS-8 · H2 · ✨` **Estimate `MODEL_TIMEOUT` from content length** (user, 2026-08-07). The fixed timeout (1200s, OPS-7) is blunt — too short for a huge generation, needlessly long (hangs) for a tiny one. Scale it to the expected work: generation time correlates with material length (input) and output scope (N groups × variants for quizzes, N sections for pages). **Approach (rough → measured):** (a) input-proportional — `base + per-1K-chars`, cheap + deterministic; (b) output-scope-aware once the concept/question count is known; (c) **measured per-model throughput** (tokens/sec), logged from actual run wall-clock + content size in the run-store, then `timeout ≈ expected_tokens / throughput × margin` — the "measure, don't assume" version, per-model because throughput varies by model/hardware (same discipline as `max_pass_chars` and the page router's measured length). An underestimate is no longer catastrophic — a timeout now fails CLEAN (OPS-7) — so this can start as a **warning/suggestion** ("big source — consider `MODEL_TIMEOUT=X`") before auto-setting it. Ties to OPS-7, the composable-generation measured-budget theme, and PAGE-4/route's measured-length decision.

**Cross-cutting themes:** composable generation (PAGE-4/8/9, COMP, PROF-3) · coursekit as advisor not just generator (CRSE-4, EVAL-10, PROF-5) · human-in-the-loop curation (PAGE-7, EVAL-7, UX-1) · rich pedagogically-framed content (RICH-*, EVAL-12, PAGE-11) · front-end & product form (UX-2/3/4, SVC, RICH-6).

## Detail — dated history (was: parking lot)

The full record for each outline item, with its dated decisions. Each is tagged with its ID from the outline above; the outline is the structured view, this is the archive. Captured, deliberately not active — reviewed at each regroup.

- `ASMT-4/5` **assignments and discussions** need to be added.
- `ASMT-2` **Quiz introduction** needs reworking. Very generic and factual but doesn't give a good reason for taking the quiz (other than it being mandatory). Need to explore more engaging quiz forms within the remit of canvas.
- `ASMT-3` **A different assessment instrument for ENDURING UNDERSTANDINGS** `[generate/evaluate]` (user, 2026-08-02) — enduring understandings are inherently hard to assess with multiple choice, and especially badly served by it: MC rewards recognition of discrete facts, but an enduring understanding is a transferable, meaning-making insight (the UbD macro-level above the concept map). Forcing it into an MC group yields vague or mis-formed stems — **confirmed this session**: the `enduring_understanding` groups drew a disproportionate share of the quiz flags (`g6/A` "grammatically incomplete/nonsensical", etc.), and the flexible-quiz-count feature currently makes one MC group *for* the enduring understanding, which is exactly the thing MC can't test. This is authentically UbD (Wiggins & McTighe pair enduring understandings with **performance-based** assessment — the six facets / GRASPS tasks — not selected-response). **Direction:** a distinct assessment type keyed off each week's `enduring_understanding` (already in the concept map), producing an open-ended task + rubric rather than `bank.json` MC variants. Seed ideas from the user (grist, not exhaustive): **Performance Tasks** — solve a real problem using the knowledge; **Transfer Goals** — apply the core idea to a completely new situation; **Explanation & Defense** — teach the concept or explain in their own words why it matters. Implications: it's a new artifact/emitter (task + rubric), not an MC bank; open responses are **instructor/human-graded** — coursekit produces the task and rubric, never grades student work (keeps the [instructor-content-only] boundary). Near-term option while this is parked: stop minting an MC group for the enduring understanding (make it optional / drop it) so the quiz stops carrying the question type it's worst at. Relates to the concept map's `enduring_understanding`, [[assignments and discussions]], and the quiz-voice flag-rate finding.
- `RICH-1` **Studio, Bauhaus and Plotter Styles** they add bullet points on line breaks in the comparison columns, but with code that looks wrong. Also no indents preserved. ![unwanted bullets](<screengrabs/Screenshot 2026-07-29 at 3.32.33 PM.png>) Needs a better way of displaying comparisons other than bulletted lines.
- `EVAL-1` **Concept-delivery v2** `[evaluate]` — pin concept _extraction_ to the material (counts wobble now); optional course-authored concept list.
- `EVAL-6` **Multi-model reads** `[evaluate]` — for borderline facticity cases the single critic is a coin-flip; a second _model_ (not seed) would let the union help. Seed-diversity is a proven no-op on the local model.
- `EVAL-4` **Evaluator blind spot: quality vs presence** `[evaluate]` — a real week-7 "details" Q&A (a muddled, partly-wrong retrieval prompt: spurious array framing; `undefined` vs `NaN` confused) passed all three checks. Two gaps: (1) the pedagogy rubric scores a hook/retrieval/contrast for _existing_, not for being _good_ — a present-but-hollow device still gets 3/3, and the loop-back added more such devices; (2) facticity misses a _near-miss_ — a muddled-but-on-topic answer (right vocabulary, wrong reasoning) reads as plausible. Human review still catches these; the critic is a first-pass filter, not a replacement. **New failure mode (2026-08-07, from the PAGE-7 measurement): the critic FALSE-flags a CORRECT block when the MATERIAL contradicts itself.** Week-8's `.setRed()` code was fixed to the right in-place form, but the transcript teaches setRed *both* ways (an authoritative "The Correct Implementation" section says re-assign; a later "Correct:" section says mutate in place), so the critic — which judges "does this match the material" — kept flagging the correct code and the fix loop reported "could not fix" a block that was already right. The critic has no notion of a self-contradictory source; it can't prefer the settled claim. A candidate signal: flag INTERNAL material contradiction itself (two sections asserting opposite things) rather than pinning it on the generated block. Ties to PAGE-7 (teaching-is-temporal) and the transcriber marking settled states. **Knowledge-type caveat (user, 2026-08-07):** a contradiction-aware critic must NOT flag all contradiction as error — DECIDABLE knowledge (code, math, fact) has a right answer (contradiction = a defect to settle), but INTERPRETIVE knowledge (a poem reading, an art critique, competing theories, an ethical argument) sustains multiple valid contradictory positions (contradiction = legitimate plurality, often the pedagogical point). The critic must be aware of a claim's epistemic status (decidable vs interpretive, tracking the domain/knowledge type) or it will flatten valid plurality into a false single answer. Full articulation in the **transcriber roadmap `EVAL-3` (knowledge-type-aware cohesion)**.
- `EVAL-2` **Development-adequacy / coherence dimension** `[evaluate]` — the metrics reward structural _presence_ (headings, a retrieval prompt) and _nominal_ coverage, but miss over-compression: week-7 crams functional-abstraction → arrays → array-manipulation → arrays-of-objects → parallax with no transitions and no concrete array example, yet scores pedagogy 14/15 and concept-delivery 3/3. Add a dimension for whether dense content is adequately unpacked, connected, and load-managed (CLT: intrinsic load, segmenting, coherence). The current rubric only faintly sensed it (week-7 SIGNALING 2/3, "the one key idea is diluted").
- `EVAL-3` **Visual pass of the rendered page** `[evaluate]` — the critic reads block _text_ and never SEES the page, so a structurally broken page (week-7 `--detail full`: empty trailing headings, unreadable near-white text) passed facticity with **0 flags**. Closing the blind spot means rendering the page and evaluating it visually/structurally — a vision model on the HTML, or at minimum structural checks (empty headings, contrast/WCAG, block ordering, coherence). The concrete shape of "judge coherence, not just content."
- `EVAL-8` **Pedagogy rubric — more dimensions** `[evaluate]` — fold in representation / accessibility / restraint; sharpen the 0–3 anchors; more-extreme calibration variants.
- `EVAL-13` **Retire the hand-authored synthetic set** `[evaluate]` — the generated set scores cleaner; consider making `synthesize.py` the single source.
- `ASMT-15` **synthesize.py for GENERATION** `[generate]` — the programmatic generator may make _better_ questions than the model path; explore using it for generation, not only eval. (Look closely, deliberately parked.)
- `ASMT-16` **Visual / image-bearing question types** `[generate]`.
- `PAGE-4/8 · theme` **Composable generation — the attention/crowding answer** `[generate/arch]` (user, 2026-07-31) — instruction crowding is a first-order, GROWING concern: the monolithic single-shot page generation overloads a small model's finite attention, and every capability we add (images, concept checklist, contrast, one-idea) taxes the same budget. **Already biting** — the same root cause explains the fading retrieval foldouts, errors clustering later in a long generation, and the `--detail full` runaway. **Direction:** decompose generation into focused sub-tasks, each with a tight instruction set + small context, so crowding can't happen — and the **concept map is the natural decomposition seam** (it is already the plan): one pass for the frame (hook + enduring understanding + recap from prereqs), one focused pass PER concept (its material + KCs + "teach this one thing well"), one for the close (summary + retrieval), then a light assembler. **Two separable axes:** (1) decompose the GENERATION (attacks attention); (2) emit MULTIPLE pages per week — per concept or grouped — which segments pedagogically (CLT), aids navigation, AND is authentic to the real course (the ARGS260 module is many wiki pages, not one giant page); generating per-page gives both benefits at once. **Validate empirically first (the project's calibration discipline):** an A/B — same week generated monolithically vs concept-map-decomposed — scored by the three existing evaluators (facticity error rate, pedagogy RETRIEVAL, concept-delivery). That measures whether decomposition lifts quality before a big rewrite. **Cost:** a real architecture change (Generator seam runs one conversation per unit today; this needs several sub-conversations + an assembler + a coherence/through-line layer, more model calls but each smaller/faster/higher-quality). This is the scaling answer for staying quality-competitive on LOCAL models as the tool gets richer. **Sharpened 2026-08-04 (session evidence + an assessment plan before the rewrite).** Separate TWO burdens, because they have different fixes: (1) **instruction load** — how many standing rules + how much transcript sit in the prompt (domain, voice, concept checklist, images, contrast, retrieval gate all compete for the same attention); (2) **output scope** — how much the model must emit in one uninterrupted pass (a whole page of N sections, a whole bank of N×4). Reducing (1) = trim/route instructions; reducing (2) = decompose the output; do both. **Fresh evidence it's biting now, not later:** the page heading-order break (all headings dumped at the end, appeared only once voice added load — the model shed a hard structural rule); the quiz form-mismatch (a conceptual "why/benefit" stem paired with code-snippet options) that PERSISTED across regens; the hook-anchoring (latching onto the one in-prompt example = load-shedding); and a **negative A/B** — scoping voice out of quiz stems did NOT lower the flag rate, so voice was not the driver, which itself argues the burden is STRUCTURAL (juggling the position rule + type mix + coverage + distractor plausibility at once) rather than any single instruction. **Modularization levers beyond per-concept passes:** (a) **route** instructions to only the blocks that need them (voice→prose not code, contrast→only a wrong-vs-right, retrieval→once at the close); (b) **move deterministic work out of the model** — the "half must be MC" type mix (assign up front), the correct-index rotation (shuffle options to the target index post-hoc), section assembly (the model already fails it), glossary/key-terms placement — every deterministic thing removed frees attention for the only thing the model is needed for, judgment; (c) **feed sharper material** — a concept's own slice/KCs, not the whole raw transcript. **How to assess what context earns its place (measure, don't guess) — the four probes:** (1) **ABLATION via the run-store** — remove one component, regenerate, measure the flag/pedagogy delta; removal that doesn't hurt = dead weight, removal that helps = active crowding (the voice scoping was an informal ablation, and it came back negative); the archived `evals/` runs are the substrate. (2) **ERROR-POSITION analysis** — flag rate vs. block index (from `calls.jsonl` order + the review files); quality decaying with position ⇒ the output is too long for one pass ⇒ decompose (tests burden #2); cheap, no model needed, runs over existing runs. (3) **INSTRUCTION-ADHERENCE probes** — how often each hard rule (heading interleave, index rotation, option-type match) is obeyed early vs. late; a rule kept at the top and dropped at the bottom is being crowded out, and names WHICH instruction is fragile. (4) **TOKEN accounting** — the prompt's share that is transcript vs. standing instructions vs. domain vs. voice vs. concept map; shows the shape of the burden. **Plan: measure before rewriting.** Run ablation (#1) + error-position (#2) first (cheap; #2 needs no model and works on runs already generated), harvest the deterministic wins now (type mix / index rotation / assembly — safe and immediate), THEN commit to the per-concept decomposition with numbers in hand rather than a blind rewrite. The coherence/through-line layer is the real cost of decomposition (a per-concept pass loses the global view). Local-specific: a frontier model would swallow the monolithic prompt; a ~26B local model has a fixed attention budget, so this is the scaling path for staying local-first as capabilities grow, not a tidiness nicety. **Probe results 2026-08-04 (evals/errpos.py + evals/tokacct.py, no model).** _errpos_ (output scope): too few archived flags to conclude (9), one weak lean toward late-group elevation that survives the enduring-understanding confound. _tokacct_ (instruction load) is more decisive — the generation prompt is **80–92% transcript** (quiz avg 86%, page 80%); shipped instructions are only **7–12%**, voice **4%**, domain **1%**, concept map **2%**; and the instruction share SHRINKS as the transcript grows (week-3 33K chars → 15% instructions; week-8 128K → 4%). Two conclusions: (1) **trimming/routing instructions is a small lever** — the rules are already a sliver; cutting them barely moves the prompt. (2) The real burden is **material volume + output scope**, so the lever is DECOMPOSITION — feed each pass one concept's slice of the transcript (the concept map already carries per-concept `key_material`), not the whole 30–128K-char week. And the small instruction share is itself a crowding argument, not a reassurance: a critical rule (interleave headings, match option type) is a needle in an 80K-char transcript haystack, easy to lose late — decomposition shrinks the haystack around each rule. Net: prioritise per-concept decomposition over instruction-trimming; the deterministic wins (move type-mix / index-rotation / assembly out of the model) still help at the margin and are safe to harvest first. **Prototype built + iterated 2026-08-04** (`coursekit/generate/page/decompose.py`, `prompts/page/{frame,concept_section,close}.md`, `--week`/`--review`/`--no-neighbours`, writes to `pages-decomposed/`): frame → per-concept → close passes into the shared IR; concept + summary headings placed DETERMINISTICALLY (the name is known — one section per concept, correct order, and the heading-order failure is impossible); a prev/next coherence WINDOW (neighbour name+gist only, not material — keeps the token win, avoids bleed); a nudge in the pass driver (the small model narrates instead of calling tools). On week-7 it went from a broken monolithic page to 56 blocks, 7 uniform sections, finalized, ~4% facticity flags. **Open boundary problem (user, 2026-08-04) — the real limitation.** The per-concept material is picked by a KEYWORD slicer (`slice_material`), which is retrieval, not SEGMENTATION: a concept's worked example usually doesn't repeat the concept's name (the code follows "let's see it:"), so the example chunk scores 0 and is dropped while the keyword-rich definition is kept — the section arrives with a definition and no demonstration, and (pre-guard) the model invented one. Contiguity is ignored too (definition + example are adjacent but scored independently). **Principled fix: stop re-deriving boundaries at generation time — have `analyze` record each concept's MATERIAL when it builds the map** (the model that identifies concepts already knows which stretch taught each: definition + example + code as one unit). Store the verbatim code/examples per concept (the fidelity-critical part keyword-slicing loses) — compact alternatives are char offsets (models unreliable at these) or a short verbatim anchor phrase per concept, spans located in order. This is the SAME artifact "Multi-source convergence" wants (per-concept material across sources), so they converge: the concept map should carry each concept's material. Cheap interim without touching analyze: make the slicer contiguity + code-aware (expand around the peak chunk to adjacent chunks; always pull in code-fence chunks near a concept's mentions) — recovers most examples, a heuristic not a solution. **Boundary segmentation BUILT 2026-08-04** (`coursekit/generate/page/segment.py`, `prompts/page/segment.md`): analyze partitions the week text into one contiguous verbatim span per concept (the model marks, in concept order, where each concept's teaching begins; the spans between marks partition the transcript), stored beside the map as `week-<key>.materials.json`; decompose reads it (`[map]`) and falls back to the keyword slicer per concept (`[slice]`). Boundary-correct + material-kind-agnostic. Assumes concepts are taught roughly IN ORDER — which is the seam for the next item.
- `PAGE-9` **Representing a non-linear / revisiting lecture in a page** `[generate/arch]` (user, 2026-08-04) — the decomposition + boundary segmenter assume concepts are taught roughly IN ORDER (one contiguous span each); a lecture that SPIRALS — teaches A, then B, then returns to A deeper — breaks that, and the current segmenter misses the later return. The open design question: how should a page REPRESENT a non-linear lecture, and it should be a CHOICE (likely instructor-selectable, like detail/theme). Options: **(a) Consolidate** — one section per concept gathering ALL its material wherever it appears (first pass + returns) into one coherent treatment; needs chunk→concept LABELING (non-contiguous gather) rather than the contiguous boundary-marking segmenter; presents each concept whole but flattens the lecture's spiral pacing. **(b) Preserve the spiral** — the page follows the lecture and a revisit becomes its own later section ("Returning to A — going deeper"); honours spiral/retrieval pedagogy (the return is intentional), but a concept then appears in multiple sections, which the one-section-per-concept model doesn't fit (needs a notion of concept "passes"/levels). **(c) Layered depth** — one section per concept, the later return folded in as a progressive-disclosure sub-block (a "Going deeper" `details`) — consolidation, but keeping the depth distinction. **(d) Graph + cross-links** — make the non-linearity explicit: sections cross-reference ("we return to this in §5") via anchors, following the concept map's prereq/`teaches_toward` edges (the map is already a graph). **(e) Detect + choose** — flag a concept whose material spans are NON-CONTIGUOUS (a revisit) and pick a structure per course/concept: merge / spiral / layer. **Mechanism note:** (a)/(c)/(e) need the segmenter to support non-contiguous per-concept material — chunk→concept labeling is the natural generalization of `segment.py`'s boundary-marking, and it also handles out-of-order teaching. Ties to composable generation, pluggable pedagogic strategies (spiral-vs-linear IS a pedagogy strategy), the concept-map-as-graph, and prerequisite gap analysis.
- `PAGE-8` **Decomposition — open questions from the first good page** `[generate/arch]` (user, 2026-08-04) — the boundary-segmented page came out well-formed, on-voice, drawing real code examples, but MUCH longer than the monolithic page (more a faithful REPRESENTATION of the lecture than a tight teaching page). (a) **Length is unwired** — the decomposer ignores the `detail` (brief/medium/full) knob; each concept pass teaches its whole span freely, so length is emergent. Decide whether "full representation" is the right default (great as a study/reference resource, maybe too long as a scannable intro) and wire a per-concept length control if not. **RESOLVED 2026-08-06:** `--detail` was REMOVED — a per-concept pass can't judge brevity without seeing the week's extent, so a length dial there was incoherent. Length is now a FUNCTION choice (overview / glossary = short by job; teaching = full); a genuinely-shorter *teaching* page is the deferred depth-planner (rank/allocate from a whole-week view), not a per-concept flag. See "Consolidate the page generators". (b) **Sub-unit granularity** — today week = one page, but the week text is a concatenation of the week's VIDEOS (the transcriber already emits per-video artifacts), so pages could decompose at the VIDEO or topic level, not just the week: authentic (the real ARGS260 module is many wiki pages ≈ per video), more navigable (one video = one page), shorter, and it naturally carries the video embed + timestamps. The unit of decomposition (week/video/concept/module) is itself a choice worth exposing — this sharpens the parked "multiple pages per week" idea to "per video." (c) **`pages-decomposed/` is a PROTOTYPE A/B location**, not production — it sits beside `pages/` so nothing is clobbered while comparing; if decomposition wins it either replaces the monolithic generator (writes `pages/`) or becomes a selectable mode. (d) **Span balance** — boundary segmentation can hand one concept a huge share of the transcript (week-8 gave "Autonomous Agents" 56K of 118K chars in ONE pass), re-approaching crowding within a single pass; a dominant span signals a too-coarse concept map (33 KCs → 6 concepts) and/or the case for per-video decomposition. Watch span sizes; balance or sub-split when one concept dominates. **RESOLVED to sub-splitting 2026-08-04** — week-8's "Autonomous Agents" (56K of 118K) produced +0 blocks (the oversized single-concept pass re-crowded and generated nothing). A truncating cap was rejected (user): it silently drops the tail, the model isn't told it's partial so it presents an incomplete concept as complete, and a mid-cut risks a broken fragment → fabrication — trading a LOUD, guard-caught empty section for a QUIET partial one. Built instead: `_material_chunks` sub-splits an oversized span into ≤budget chunks on paragraph boundaries (nothing dropped, each pass sees a complete chunk, later sub-passes told to CONTINUE the same section under the one heading). **The budget is MEASURED, not assumed** — `max_pass_chars` in page.yaml, default 20000; the right value is the model's usable per-pass context/attention limit, which is task-dependent (this model: a focused pass handled ~30K but failed at 56K, while the monolithic prompt held 118K) and should be measured per model, not hardcoded. **Per-video PARKED with a key insight (user, 2026-08-04):** per-video does NOT escape this — a video's length is as unbounded as a week's; both bottom out on the same LLM attention/context limit. The unit (week/video/concept) is a navigation choice, but "bound content to a MEASURED capacity" is the invariant underneath all of them. So per-video is still wanted (navigation/length/authenticity/timestamps) but it is not the fix for oversized content — sub-splitting-to-a-measured-budget is, at whatever unit.
- `EVAL-7` **Correctness vs style — "clunky but working" as pedagogic value, not a flag** `[evaluate]` (user, 2026-08-04) — the facticity critic flagged working-but-clunky code (a `move()` method that also draws — should arguably be `update()` or split) as "incorrect". It is NOT wrong, so it is a false positive on correctness — but the observation is pedagogically REAL. Two moves: (1) tighten the facticity critic to separate INCORRECT (won't run / contradicts the material) from SUBOPTIMAL/clunky (works, not ideal), so style stops firing as a facticity flag; (2) better — capture the style observation as a NOTE rather than discarding it: an instructor-facing pedagogy note ("`move()` mixes movement + drawing; consider `update()`") or an inline student tip — turning a false positive into a feature, and honest to how teaching actually works (clunky-but-working is a teachable state, not an error). Ties to the pedagogy evaluator, the block-edit/vet UI, and the "quality vs presence" blind spot.
- `PAGE-7` **Teaching is temporal — show the settled conclusion, not a transient state** `[generate/analyze/arch]` (user, 2026-08-04) — a decomposed page reproduced VERBATIM a transient buggy code state (`fill(this.fill) // apply the gradient`, which doesn't work) that the lecture CORRECTED later in the same source (`drawingContext.fillStyle = this.fill`). The critic correctly flagged it (true positive — the net worked). Root cause: teaching is iterative (wrong→right, naive→refined) and the transcript captures every state, but a PAGE should present the settled/working conclusion, not an intermediate. The correction IS in the source, so the transcriber did not lose it — the fix is mostly downstream. Layers, cheap→deep: (a) **page-gen PROMPT** — "when an idea evolves (buggy/naive then corrected), teach the FINAL working version; use an earlier wrong version only as an explicit, labelled contrast, never as the working example" — requires segmentation to include the correction in the concept's span (may not have; check). (b) **FIX LOOP** — the critic already flagged it and the correction exists later in the material, so `fix` could regenerate the block with the corrected code (self-heal via the net that caught it). (c) **TRANSCRIBER** — the vision analysis samples every ~10s indiscriminately, faithfully recording transient states but not MARKING which is settled; it could annotate code evolution or "settle" a code block to its final form so everything downstream sees the conclusion (bigger, transcriber-side, the most principled). Read: (a)+(b) are the tractable near-term levers; (c) is the deeper improvement. A genuinely hard temporal→static translation problem, and the productive edge of the "quality vs near-miss" evaluator theme. **Fix-loop test 2026-08-04 — NOT solved, but it sharpened why.** Ran `--fix` on week-8: the fixer reached "not incorrect" but not "the settled conclusion" — for the clunky `move()` it reached BACKWARD to an earlier WORKING version of the class (a different timeline point, not the intended final form); the buggy gradient became code-LESS prose (the no-invention guard omits code rather than show the wrong version or find the right one). Both are correct-enough — which is the crux: the facticity critic CANNOT distinguish an earlier working version from the final one (both PASS), so "show the settled conclusion" is a HIGHER bar than "not wrong" and needs a signal the critic doesn't have; the fixer stops the moment it is not-wrong, and not-wrong has many members. **Concrete lever surfaced: prefer the LATEST/settled form of an evolving idea** (recency ≈ conclusion) in generation + fix, rather than "any working version" — flips both misses (reach forward to the corrected `move()`; show the working `drawingContext.fillStyle` gradient instead of omitting). Cheap, imperfect (the last mention isn't always the conclusion), a real default. So: the facticity NET + fix loop are solid (that half is done); what's unsolved is CURATION to the best form — recency heuristic, or the transcriber marking settled states, or human vetting (block-edit UI). **MEASURED 2026-08-07 (model loaded — a valid run, unlike an earlier not-loaded one that misreported).** `fix` on week-8: the gradient bug (`fill(this.fill)`, which the transcript corrects CLEANLY — an explicit `drawingContext.fillStyle = this.fill` + a whole troubleshooting section) **FIXED ✓** — the reach-forward fix loop settles clean corrections. **PAGE-7's fix-loop lever is DONE for the clean case; no "settle-aid" build is needed.** The two `.setRed()` flags did NOT settle, and reading the transcript + the resulting `page.json` shows why, and it's UPSTREAM: **the transcript CONTRADICTS ITSELF** — a section titled "#### The Correct Implementation" authoritatively teaches the WRONG semantics (setRed "returns a new color object", re-assign `this.stroke = this.stroke.setRed(...)`), while a later "Correct:" section teaches the RIGHT one (setRed mutates IN PLACE, don't re-assign). Outcome on the page: the CODE block came out **correct** (in-place `this.stroke.setRed(...)`) but the PROSE block came out **wrong** (reassign/returns-new, pulled from the authoritative-but-wrong section) — an internally-contradictory page (prose vs its own code). Downstream generation+fix **fundamentally cannot resolve a source that asserts both**; the fix is the transcriber/source marking settled states (layer c) or human vetting (UX-1). **Evaluator corollary (→ EVAL-4):** the facticity critic FALSE-flagged the now-correct code (it "could not fix" a block that was already right) because the material contradicts it — the critic can't tell "block wrong" from "block right, material self-contradictory".
- `UX-1` **Block-level edit + re-run + a vetting UI** `[deliver/ui]` (user, 2026-08-04) — a decomposed page contained a weird, faintly patronising analogy ("keeping track of a classroom by giving every kid a notebook… you lose track"). The right answer is NOT more rules for specific analogy types (whack-a-mole); it is letting the professor VET and fix content — an edit function (with UI) and/or a per-BLOCK re-run, so a user can regenerate or hand-correct ONE block without touching the rest. The `fix` loop already regenerates a block in place; this is its user-initiated, UI-surfaced sibling. Human-in-the-loop vetting is the point; belongs in the eventual front-end.
- `SHIP-2` **Video + timestamp references on pages** `[generate/deliver]` (user, 2026-08-04) — a page should be able to reference its source videos and their timestamps (the transcriber has per-segment timecodes; a per-video page above would carry them naturally). Constraint the user raised: the page ships to Canvas, so the embed + deep-link-to-timestamp must survive the `.imscc` round trip — a real functionality limit to design against (Canvas page/file linking, iframe embeds, timestamped URLs).
- `SHIP-1` **Bundle instructor files into the `.imscc` + the manifest link↔object mechanism** `[deliver]` (user, 2026-08-04) — let a course include/upload its own files (videos, readings, images) INTO the final `.imscc`, with pages referencing them. Requires understanding how the Canvas export manifest records the relationship between a LINK and the file/object it points to — a referenced file must actually be IN the export for the link to resolve. Investigate the manifest's resource/file wiring (the real ARST260 export is the ground truth in `reference/`), then extend the CC emitter to pack supplied files + emit the manifest entries binding a page link to the packaged file. Ties to the supplements system (today only external URLs) and `emit/cc.py`.
- `PAGE-10` **Page-type taxonomy — the space of page functions** `[generate/arch]` (user, 2026-08-04) — before building more page generators, treat a page as a point in `(scope × role × source)`, not a named type (fits the flexible `page_type`). **Axes:** scope = course / module / week / VIDEO / concept / skill; role = orient (wayfinding) / frame (motivate) / teach / practice / assess / review / reference / administer; time = before (advance organizer) / during / after. **Catalog by role** (the user's six folded in): ORIENT — course home, module overview (#1), week "Start Here" (#2), course map; FRAME — advance organizer, objectives page, "why this matters", assignment brief (#6); TEACH — full week page (#5), per-video companion (#4), per-concept page, worked-example / tutorial, case study, reading guide; PRACTICE — ungraded set, "try it" interactive, lab/studio, discussion-prompt framing; ASSESS-adjacent — assignment/project guidelines, rubric-as-page, study guide, portfolio; REVIEW — week/module review (#3), cumulative review, common-mistakes / FAQ, summary diagram; REFERENCE — glossary, cheat sheet, resource list, tool-setup, examples / exemplar gallery; ADMINISTER — schedule, policies, get-help, announcements. **Canvas structural reality:** the MODULE is the real container (an ordered mix of pages / assignments / quizzes / discussions / files / external-LTI / text-header dividers); recurring macro-structures — a "Start Here" module + weekly/unit modules + a persistent "Resources" module; flipped (pre/in/post); backward design (objectives → content → practice → assess); requirement/prerequisite gating. (Moodle: sections/topics + Books; Coursera/edX: lessons of interleaved items — same idea.) **coursekit lens — cheap vs gated (the prioritisation):** the ORIENTATION / REVIEW / OVERVIEW pages are the cheapest high-value wins — low-content, high-navigation, and the data already exists (`context.yaml` has weeks/modules, concept maps have per-week topics, supplements have the links) → nearly an EMITTER/assembly job, not generation. A REVIEW page (#3) is ~free (the prior week's enduring understanding + retrieval `details`). TEACHING pages (#5 / per-concept) are the current track (need the concept map). ASSIGNMENT (#6), reading guides, and exemplar galleries are GATED on inputs coursekit does not ingest yet (assignments — see "assignments and discussions"; instructor files — see "bundle instructor files into the `.imscc`"). **Recommended next high-leverage generator: the week/module OVERVIEW pages** — near-free assembly over `context.yaml` + the concept maps, and every course needs them. Ties to composable generation (the per-video/sub-unit unit-of-decomposition question), the decomposition open-questions, and course-scope.
- `PAGE · SHIPPED 2026-08-06` **Consolidate the page generators + unify the output flow** `[generate/arch]` (user, 2026-08-04) — there are now THREE page-producing paths and they don't share a spine, which is fine while experimenting but is the next architecture step to resolve. (1) **Monolithic `PageGenerator`** — production, implements the Generator seam + driven by `pipeline.loop`, writes `<course>/pages/<week>/page.json`, and is what `evaluate`/`fix`/`emit` (CC/HTML/.imscc) actually read. (2) **Decompose** (`generate/page/decompose.py`) — the better generator per the A/B (structure + facticity), but a `python -m` prototype with its OWN multi-pass orchestration that BYPASSES the Generator seam (frame → per-concept → close, not one `pipeline.loop` conversation), its OWN inline `--fix`, writing to a SIDE tree `pages-decomposed/` that `evaluate`/`fix`/`emit` never see. (3) **Overview** (`generate/overview.py`) — deterministic assembly, writes `<course>/overview/<slug>.json` (not `page.json`) + HTML, also outside the emit/evaluate flow. **The decisions to make (soon, not mid-experiment):** (a) **which generator is canonical** — the A/B favours decompose, so the trajectory is decompose → production: does it REPLACE the monolithic generator or become a selectable mode (`generate --pages --decompose`, or a page.yaml key)? (b) **the seam divergence** — decompose is multi-pass and the Generator seam is one-conversation, so either the seam grows to drive multi-pass generators, or decompose stays a parallel orchestrator that the driver knows about; pick one so there aren't two unrelated generation mechanisms for the same `page.json`. (c) **output convergence** — everything should land where `evaluate`/`fix`/`emit course` see it uniformly: one canonical `pages/` tree, `page.json` naming (overviews use `<slug>.json` today), so one review/repair/package pass covers ALL page types and overviews actually reach the `.imscc`. _(Partial 2026-08-04: decompose now has a `--canonical` flag that writes to `pages/<week>/`, so `evaluate`/`fix`/`emit` already work on a decomposed page; and a `--detail brief|medium|full` length knob mirroring the monolithic generator. Confirmed the decompose `--fix` calls the SAME `fix_one_block` as production — no fix logic to reconcile, just the tree it runs on, so the inline `--fix` is redundant once `--canonical` is used.)_ (d) **one fix path** — decompose's inline `--fix` vs the production `fix_course_pages` over `pages/` should become one. (e) **promote invocation** — decompose + overview are `python -m` entries; make them first-class `coursekit` subcommands/modes once (a)-(d) are settled. **Principle:** hold the canonical-IR discipline — every path converges on `page.json` → one renderer → one emitter (like `bank.json`); the resolved design lands in `agent/architecture.md`. This is the page analog of "the quizbot→coursekit rename is triggered by generator #2" — here it's "page generator v2 (decompose) earns its place as the trunk." **DECISION CRYSTALLIZED 2026-08-06 (user) — this is now the next focus.** The user-facing axis is **FUNCTION + quality**, never the generator mechanism: a user picks WHAT a page is for (overview / glossary / teaching / …) and expects it accurate and in the right voice (and maybe more parameters); WHICH generator runs is an INTERNAL decision the program makes automatically, with a `--generator auto|monolithic|decompose` escape hatch. Concretely: (i) **retire the monolithic `--detail` too** (the decompose one is already gone) — length is a function, not a dial: brief→overview/glossary, full→teaching, and a genuinely-shorter teaching page is the depth-planner, not a flag. (ii) **wire decompose into `coursekit generate --pages`** (no more `python -m`). (iii) **auto-select by MEASURED length** — a week that exceeds a single monolithic pass's reliable budget (≈ the tuned `max_pass_chars`) routes to decompose, else monolithic; deterministic, no model call, predictable, reusing the budget already measured for sub-splitting. So "which generator is canonical" resolves to BOTH, chosen per-content — the **trunk is the router**. Open sub-questions still to settle when built: (b) the seam must drive a MULTI-PASS generator (decompose bypasses the one-conversation seam today); (c) output convergence (glossary/overview now land in `pages/` too, so this is nearly there); (d) collapse decompose's inline `--fix` into the production `fix` path.
- `PAGE-2` **evaluate/fix should cover all page functions** `[evaluate]` (user, 2026-08-06) — `emit` sees every page function (all land as `page.json` under `pages/`), but `evaluate`/`fix` discover only the per-transcript-week TEACHING page (`find_units(subdir="pages")` is keyed to a week's transcript), so the `-glossary`/`-overview` sibling pages are never cold-read or repaired. Fix: make page discovery rglob `pages/**/page.json` (like `PagesSource` does) instead of per-unit, so review/repair covers all functions uniformly. Small, deferred deliberately (user, 2026-08-06). Note: the pedagogy/concept-delivery critics assume a teaching arc, so a glossary/overview would want the FACTICITY check but not those — ties to the page-type-aware validation already done.
- `PAGE-1/5/6` **Glossary companion — refinements** `[generate]` (user, 2026-08-06) — the glossary FUNCTION shipped (`generate/page/glossary.py`: model-extracts terms per material chunk, harvest + dedup deterministically, page-type-aware finalize); three improvements parked. (1) **Formatting bug** — the rendered glossary shows an empty sub-header ("·  Key Terms Recap") that closes its block before the terms, then the terms follow: the page's placed "Key Terms" heading and the glossary block's own derived "· <title> Recap" kicker collide into a redundant/empty header. Small fix — suppress the block kicker when the glossary is the page's sole section (or drop the redundant page heading). (2) **Slidable size by importance** — week-8 came out 45 terms, too many. Make glossary length a user-request parameter where the MOST important terms appear in a short glossary and less-important ones are added as it grows; importance ≈ time spent on a term and/or repetition frequency (both measurable from the transcript / the transcriber's per-segment timecodes). This is the glossary's version of the **depth-planner** (the whole-view "allocate then cut" shape): rank globally, then cut to the requested length. (3) **Vocabulary typology** — distinguish kinds of term: concrete commands / keywords / API (`beginClip()`, `new`) vs abstract ideas / abstractions (instance, OOP), and possibly "big ideas" vs "practical terms of art". Generalizes across fields (every discipline separates its concepts from its terms-of-art); a typed/grouped glossary is more useful than a flat list, and the typology could feed the importance ranking in (2). Ties to the depth-planner, the concept map (already separates a concept's name/gist from its components), and voice.
- `OPS-5` **Graft as a DEV tool (not product)** `[dev-tooling]` (user, 2026-08-04) — evaluated NanoNets/Graft (github.com/NanoNets/Graft): a persistent CODE-structure graph for coding agents (tree-sitter symbols/calls/deps, no LLM, + optional cached LLM summaries) so an agent like Claude Code doesn't re-map a repo each session; ~42% token cut on large repos (SWE-bench). **It does NOT help the product/generation loop** — that token cost is 80–92% TRANSCRIPT (per `evals/tokacct.py`), i.e. prose, and Graft parses code; the correct prose analog is already what we built (decomposition + boundary segmentation + per-pass budget). Nice parallel worth remembering: coursekit's **concept map + segmentation ARE "Graft for course content"** — a persistent structural layer `analyze` builds once so the generator references it instead of re-deriving the week from raw transcript. Graft's only fit is the DEVELOPMENT loop (Claude Code editing the coursekit repo): plausibly cuts my exploration tokens, but it's a Node dev tool (outside coursekit's clean Python deps — fine, dev-only), its gains scale with repo size (coursekit is moderate), and it optimises the dev experience, not the product or the faculty user. Verdict: a dev-experience nice-to-have, not high-leverage at current size; adopt only if session-to-session codebase re-exploration feels wasteful. Tagged tooling deliberately — never a generation/product dependency.
- `PAGE · note` **Retrieval-foldout finalize gate is PROVISIONAL — revisit/remove** `[generate]` (user, 2026-07-31) — `page.validate_final` now hard-requires a retrieval `details` block so foldouts stop being dropped. But this imposes a PEDAGOGICAL requirement to solve a TECHNICAL problem (local-model attention crowding): retrieval isn't right for every page type (a pure reference/intro page), and a hard gate can be satisfied by a hollow foldout. Keep it for now; it's a stopgap, not an invariant. Remove it (or make it page-type-aware) once composable generation manages attention properly, and if pages become multi-page/typed. Code comment marks it provisional at `page.validate_final`. **PARTIALLY RESOLVED 2026-08-06:** `validate_final` is now page-type-aware — reference/orientation types (`glossary`, `week_overview`, `module_overview`, `reference`) skip the retrieval requirement (they have no teaching arc to retrieve from); teaching pages still require it. The stopgap remains only for teaching pages.
- `RICH-2` **Images / diagrams on pages — generated diagrams (next rung)** `[generate/visual]` — instructor-supplied inline images shipped (see Shipped). The bigger step: the model AUTHORS a diagram itself (SVG/mermaid) for structural concepts (coordinate systems, data flow), no instructor file needed. Continues the visual/design-forward track.
- `PROF-4` **Composable domain bases** `[profiles]` — ship generic bases (`computer-science`, `digital-art`, …) a course selects one or MORE of and refines; assume multi-domain courses.
- `PROF-5` **Domain-suggester tool** `[profiles]` — scan the transcripts/ingestables → propose a starter `domain.md`.
- `FLOW-2` **Multi-source convergence for the concept map** `[analyze/arch]` (user, 2026-07-30) — a real course mixes video + readings + slides, but `analyze` today uses the RICHEST SINGLE source: if any `knowledge.json` exists (video-only — only the transcriber makes it) it builds from those KCs and never reads the week text; else it falls back to the text. So a MIXED week reflects the videos only — readings/slides ingested as text (coursekit ingest produces week text, NO knowledge.json) are invisible. Not biting yet (each of the user's courses is single-medium today), but the architecture should answer for it. Root: the two ingest paths converge at the TEXT layer (both aim at `week-N.md`) but only the transcriber produces the STRUCTURED knowledge layer. **Principled fix (same canonical-form discipline as `bank.json`, one level up):** every ingest adapter emits the SAME per-source knowledge artifact, each tagged with a `kind` (video/reading/slides), and `analyze` aggregates across all of them — medium-agnostic. The seed already exists: `build_concept_map_from_text`/`extract.md` turn text → the concept schema; run that PER-SOURCE (per document/transcript) rather than per-week-text and the existing aggregator unions it. Cheap interim: stop short-circuiting — feed knowledge.json KCs AND the week text to consolidation (but the week text isn't a guaranteed union either — ingest writes `output/week-3.md`, transcriber `output/week 3/week-3.md` — same convergence problem at the text layer). This also finally makes "the week text is the union of all sources" true at both layers.
- `PROF · SHIPPED; remnants PROF-6/EVAL-5` **Voice / tone profile** `[profiles]` (user, 2026-07-30) — the generated prose (and especially the enduring understanding, e.g. "Complexity is managed by organizing data into structures that allow a single logic pattern to govern many individual entities") reads more formal/academic than the user teaches. A THIRD composable prose layer beside domain (WHAT) and pedagogy strategy (HOW): a `.vtconfig/voice.md` (tone/register/second-person/idioms/playfulness) prepended to generation + consolidation like `domain_preface`. **Sharp insight:** the authentic voice is ALREADY in the transcripts — the formality is INTRODUCED by cleanup passes (the transcriber's format stage "fixes grammar, removes filler"; page generation writes formal written prose), sanding off the spoken register. So (a) an authored `voice.md`, and (b) a **voice-extractor** (like the domain-suggester) that characterizes the instructor's register from the raw transcripts → a starter `voice.md`, are both viable, and (b) works precisely because the voice pre-exists cleanup. User: "let's look at it sometime." **Blocks the concept-map-parametrized titles below — that idiom IS this profile.** **Boundary decision (user, 2026-08-02): the EXTRACTION belongs on the transcriber side, the APPLICATION on coursekit — a split, not a wholesale move.** Extraction (raw transcript → register features → `voice.md`) is a _source characterization_ exactly like `knowledge.json`: it must run on the RAW side of the transcriber's own pipeline, because the register lives in the raw ASR text and its cleanup/format pass degrades it (the transcriber already keeps a raw form — the prototype source was `…​.raw.md`); spaCy is a marginal dep there (already whisper/torch-heavy) but a new dependency class in coursekit (deliberately light: pydantic/openai + stdlib, no video stack); and the two tools already meet only through `.vtconfig/` files, so `voice.md` joins `domain.md`/`knowledge.json` (transcriber writes, coursekit reads via `courseconfig`, neither imports the other). Application — inject `voice.md` into generation + consolidation prompts (`domain_preface`-style) and score prose against it — is unambiguously coursekit. **Fallback nuance (mirrors the concept map's text-fallback):** coursekit needs a lighter text-fallback extractor for the document-ingest path (readings/PDF courses, no transcriber, no spoken audio) — there "voice" is written-source register and the cleanup-destroys-it concern doesn't apply; so the spaCy MECHANISM may be shared code while the rich raw-transcript invocation is the transcriber's. Voice is also a property of the PERSON (stable across their courses), a further argument for capturing it once at the source. **Prototype exists** at `~/spacy-test/app2.py` (register/voice feature profiler: cleans transcripts, foregrounds topic-independent interpersonal markers — hedges/discourse-markers/2nd-person/contractions — places each on a conversational↔academic continuum instead of noun-dominated raw sums, pulls real example utterances for the LM, takes transcript paths + aggregates). Two calibration caveats found: the reference anchors are dialogue-calibrated so a monologue lecture under-reads, and ASR under-punctuates questions — recalibrate anchors to a spoken-lecture baseline before trusting the composite index. **Before productionizing: re-read `~/video_transcription` to confirm the transcriber's exact raw-vs-cleanup stages** (per the facticity rule — claims about the transcriber are read, not reconstructed). **BUILT 2026-08-02 — both halves.** Transcriber side (read-confirmed: `vt_transcribe` writes `.segments.json`+`.raw.md`, `format` is the register-flattening cleanup): `vt_register.py` (deterministic profiler, spaCy lazy) + `vt_voice.py` (the `voice` step — aggregates raw transcripts course-wide, writes `.vtconfig/voice-features.json` + a seed-once `voice.md`) + `prompts/voice/default.md`; default model `en_core_web_trf` (torch already present there). coursekit side: `CourseConfig.voice` + `voice_preface` (tone-only, subordinate to correctness, with an explicit precision guard for stems/answers), injected into the PROSE generators (page + quiz `context.py`) and both fix loops — but NOT the critics (voice isn't a correctness axis). A real `voice.md` was generated on ARST260 and reads authentically. **Deliberately excluded / still open:** concept-map consolidation is NOT voiced (structured planning artifact; clarity as a checklist wins, and the enduring understanding gets re-voiced when the page is written); a **voice-match evaluator** (score generated prose against the profile — the fourth evaluator, parallel to the pedagogy rubric) is unbuilt; coursekit's **text-fallback extractor** for the no-transcriber document path is unbuilt (the spaCy mechanism could be shared); anchors are still dialogue-calibrated (recalibrate to a spoken-lecture baseline). This also **unblocks the concept-map-parametrized idiom titles** below — the idiom artifact now exists.
- `PROF-2` **Concept-map-parametrized section titles in the professor's idiom** `[generate/profiles]` (user, 2026-07-31) — PARKED, blocked on the voice/tone profile above. Assessed 2026-07-31: the request is to title each section NOT with the raw concept name but by passing the concept (name/gist/level, from the map) as a PARAMETER to the model to get "an equivalent title in the idiom of the professor." **We have one half, not the other.** Present: the concept map is already loaded into page generation (`load_for_unit`), so the concept is available to parametrize a title. Missing — and it is the defining ingredient: any representation of the professor's titling *idiom* (register, voice). No voice artifact exists; the only "style" in the repo is visual design tokens (themes), which is orthogonal. Without a captured idiom the model would echo the concept name or invent a generic title — exactly what the user said NOT to do — and inferring the idiom ad hoc mid-generation reintroduces the attention crowding that produces the `》`/`>>` heading junk in the first place. **Right sequencing:** build the voice/tone profile as an editable artifact first (the analog of the concept map / `domain.md`), THEN (c) is a clean title step parametrized by (concept, idiom). This is also the deeper fix for the heading-creep symptom — a titling pass fed the concept + idiom, rather than free-authoring headings deep in a crowded generation.
- `PROF-3` **Pluggable pedagogic strategies** `[profiles]` — per course/professor; anti-staleness (one fixed style goes stale); today's hook/key-idea/consolidation fix is one _default_ strategy, not the only law.
- `PROF-1` **Document the config convention** `[profiles]` — `.yaml` = settings the code reads; `.md` = prose the model reads (`domain.md`, prompt overrides, future pedagogy profile). Put it in `docs/configuration.md`.
- `CRSE-2` **Course-level evaluator** `[deliver]` — spacing/spaced-retrieval, pacing, sequencing, cross-week coverage. (Spacing is a schedule property, so it belongs here, not on a single page.)
- `CRSE-1` **Prerequisite gap analysis** `[analyze]` (user, 2026-07-30) — the payoff that makes concept `prerequisites` worth getting right. Principle the user pinned: **prerequisites are a property of the CONCEPT (its intrinsic dependencies), not the COURSE (its coverage)** — deriving them from "what earlier weeks taught" mirrors coverage and makes gap-finding impossible by construction. So the concept map stores coverage-INDEPENDENT prereqs (the consolidation prompt now enforces non-self-referential, true-dependency prereqs); a later cross-week pass matches each concept's prereqs against the actual concept graph across weeks + a small "assumed incoming background" set, and a prereq satisfied by NEITHER is a **content gap** (the course assumes something it never taught and the student doesn't bring). This is the cross-week analysis the `analyze` phase was designed to grow into. Note: prereqs do NOT feed page generation today (only name/gist/key_material/enduring_understanding do), so weak prereqs are latent, not harmful, until this lands. Also fold in `teaches_toward` for sequencing.
- `UX-3` **Front-end / form factor** `[deliver]` (2026-07-30) — the CLI is a prototype; a friendly GUI is the highest-leverage step for reaching beyond early adopters, and eventually a packaged desktop app with Ollama bundled (so no manual model setup). The decisive usability track. Product rationale lives in the offline product doc.
- `RICH-6` **Visual / design-forward track** `[deliver/visual]` (2026-07-30) — a coherent direction, not one feature: design-forward, pedagogically-grounded visual output + a visual authoring surface is the product's most authentic differentiation (the pedagogy↔design CLT/UDL layer already exists; the concept map already IS a graph). The front-end and "design for engagement" are the SAME build. Clusters parked items into one thesis: generated diagrams on pages (images shipped; diagrams next); the visual/design-legibility evaluator (a vision model that SEES the page); a concept-map-as-node-graph editor (Novak form, replaces YAML authoring); live page preview + theme gallery; evaluation as a course-health dashboard; embedded interactive sketches (p5.js iframes). Product rationale + a visual-first mockup live offline.
- `SHIP-5` **Canvas extension / LTI** `[deliver]` — plausibility of surfacing the tool inside Canvas. CLARIFY first: _which_ chatbot — coursekit's own generation surfaced interactively, or a separate project?
- `OPS-4` **RAM pre-flight false positive** `[deliver]` (user, 2026-07-31) — `hardware.check_fit` warns "model ~14.7 GB but budget ~9.2 GB (available × 0.7), may fail to load" when loading right after ejecting another model; the model loads fine (available RAM not yet reclaimed; LM Studio evicts-then-loads + macOS swap). Advisory only (prints, never blocks). Fix: when a _different_ model is loaded, add its footprint back to `available` before the budget check (it will be evicted), or add a total-RAM factor (transcriber uses available-0.7 / total-0.35). Low priority — it's a cosmetic false alarm.
- `OPS-2` **One-off:** regenerate the one thin page (a Functions week scored low on delivery — under-generated).

- test models.  Model size against accuracy and speed. Look at the relationship between time (tokens/sec or just seconds?), context length and model size.  Create a tool that can test coursekit when hosted on an arbitrary machine and figure out an optimized path. Questions do we need a 'db' or table of databases? A hook up for huggingface from coursekit? A set of tests that test for different uses – quizzes versus pages, length of content? Different content types? Different course levels? 

### Carried over from the increment history (reconciled 2026-07-30)

Increments 1–10 are complete in code; these are the live remnants, so nothing real is lost. The offline plan file is now history.

- `FLOW-1` **Non-coding readings → page end-to-end** `[generate]` — Increment 8's acceptance gate: a real readings/PDF course (no transcripts) through `ingest → generate --pages`, confirming a sensible non-coding page. A user model run.
- `SHIP-4` **Canvas API push** `[deliver]` — Increment 3/6 Phase 3: `POST /api/v1/courses/:id/pages` with the rendered HTML; defaults to dry-run, gated on a running Canvas + token. File emitters stay first-class regardless.
- `EVAL-14` **Quiz deterministic verifier** `[evaluate]` — Increment 10 steps 4–5: execute/compute decidable questions (loop trace, arithmetic) in a sandbox instead of trusting the LLM. Deliberately deprioritised — the critic's 100% wrong-answer recall weakened the rationale; revisit only if a decidable-slice failure resurfaces.

## Shipped

A running changelog — one line per completed feature, newest first, with its commit(s).
The sections above stay forward-looking; deep detail lives in the offline log + git.
(Increments 1–9 — GIFT/QTI/CC emitters, `courseconfig`, the quizbot→coursekit rename, the design system, document ingest — predate this changelog; see `architecture.md`'s "Order of operations" and git history.)

- 2026-08-07 · **Model timeouts handled gracefully (generate side; OPS-7 progress)** — a request TIMEOUT on a slow big generation used to crash `generate` with a raw `openai.APITimeoutError` traceback (hit on a `--source` loop over week-3 decks). Now: the provider sets a generous, tunable request timeout (`MODEL_TIMEOUT`, default 1200s) so a slow-but-fine generation isn't cut off; and `pipeline.loop` treats a timeout as TRANSIENT + per-unit — it ends that artifact cleanly (INCOMPLETE, no traceback) so a batch/loop moves on, distinct from a model-LOAD error which still aborts the whole batch (`_looks_like_timeout` vs `_looks_like_model_error`). _(pending commit)_
- 2026-08-07 · **Targeted quizzes (ASMT-17 v1)** — `coursekit generate --quizzes --source "<doc>"` scopes a quiz to ONE reading/slide/PDF instead of the whole week (`generate/quiz/targeted.py`): extract the doc (`ingest.extract` — pdf/pptx/docx/odt/md/txt), give it a distinct slug (`week-<n>-<doc>`, never collides with the week quiz), persist `source.md`, run the standard generator with the course's `domain.md`/voice. The quiz twin of the page `--function` axis; the per-reading "reading quiz" a real course needs. _(pending commit)_
- 2026-08-07 · **Fix loop: infra errors abort clearly (OPS-6)** — a not-loaded / unreachable model is now caught in `fix_one`/`fix_one_block` and re-raised as `ModelLoadError` (shared `_abort_if_model_error`, reusing pipeline's `_looks_like_model_error`/`_model_error_message`), so `coursekit fix` aborts with "model not loaded" instead of reporting every flagged item as "could not fix"; content-level failures still surface per item. `render_outcomes` gains a `noun` so page runs say "section(s)", not "question(s)". Groundwork for the shared error module (OPS-7).
- 2026-08-06 · **Catalog-driven composition (COMP-3 v1, prompt-injection)** — `catalog.render_palette(content_type, only=…)` renders the component guidance (tool + function + use/avoid + variants) from the catalog; injected as `{palette}` into `prompts/page/system.md` (monolithic, full page palette) and `concept_section.md` (decompose, the section-body subset via `decompose._SECTION_KINDS`). Deleted the hand-written device lists that drifted across those prompts — both generators now compose BY FUNCTION from one source, and adding a component updates generation automatically. Changes generation output → acceptance is a user-run before/after A/B on the three evaluators. _(pending commit)_
- 2026-08-06 · **Shared block vocabulary + content-type governance (COMP-2)** — `coursekit/generate/blocks.py` is the content-type-neutral import point future content types compose from; the catalog gains `CONTENT_TYPES` + `components_for`/`allows`/`unknown_components`, so which components an artifact may use has one source (a component's `content_types`). The block class definitions stay in the page IR behind the re-export until content type #2 (extract-at-second-consumer). Drift guard re-keyed to `blocks._KINDS`; docs/components.md gains a Content-types section. _(pending commit)_
- 2026-08-06 · **Component catalog (COMP-1/COMP-4)** — `coursekit/generate/catalog.py`: a registry of every component (the 11 page blocks) with the pedagogic **function(s)** it serves (a named CLT/UDL vocabulary), when-to-use / avoid, schema + variants, and a `content_types` seam for COMP-2. `docs/components.md` is generated from it; drift-guarded (catalog ↔ `page._KINDS`) and freshness-tested. The foundation for COMP-2 (shared vocabulary) + COMP-3 (compose by function) + the upstream evaluators. _(pending commit)_
- 2026-08-06 · **Page FUNCTION axis + auto-router** — `coursekit generate --pages --function teaching|glossary|overview`; a deterministic multi-signal router (`generate/page/route.py`: length + concept count + largest span, page.yaml-tunable) picks monolithic vs decompose for a teaching page, `--generator auto|monolithic|decompose` overrides; the monolithic `--detail` is removed; decompose is wired into `generate` via a per-unit dispatcher (`generate/page/build.py` + a `unit_runner` seam on `run_course`). The user picks WHAT + quality; the program picks HOW. All functions land under `pages/`. _(pending commit; evaluate/fix still discover only teaching pages — small follow-up)_
- 2026-08-06 · **Length is a FUNCTION, not a dial** — retired the decompose `--detail` (a per-concept pass can't judge brevity without the whole extent); the teaching page (decompose) now writes only to canonical `pages/<week>/` (no `pages-decomposed/` side tree — that killed the emit-collision class); added the **glossary companion** function (`generate/page/glossary.py` + `prompts/page/glossary.md`: model-extracts terms per material chunk, deterministic harvest + dedup, renders with supplements so a video sits beside the terms); and made `page.validate_final` **page-type-aware** (reference/orientation types skip the retrieval-foldout requirement). Also: the course cartridge now fails **gracefully** on a duplicate-slug collision, naming both source files. _(pending commit)_
- 2026-08-02 · **Voice on quizzes scoped to feedback only** — after voice went live, a full-course run showed quiz flags rise (~2% → ~6%) while pages improved: a hedged register bleeds into stems/options ("asks why, but the options are syntax"). So the quiz generator + quiz fixer now use a narrower `quiz_voice_preface` (voice in feedback/explanations only; stems and options stay literal); pages keep the full voice. _(pending commit)_
- 2026-08-02 · **Generate's auto-review archives to the run-store** — the cold read after `generate` now snapshots to `<course>/evals/<timestamp>/` + `log.jsonl`, same as `evaluate`; `_review_quizzes`/`_review_pages` return `(review, metrics)`. `b298def`
- 2026-08-02 · **Voice/tone profile — both halves** — transcriber `voice` step (`vt_register` + `vt_voice` + `prompts/voice`, `en_core_web_trf`) writes `.vtconfig/voice.md` from the raw transcripts; coursekit reads it (`CourseConfig.voice` + `voice_preface`) and injects it into the page + quiz generators and both fix loops — tone only, precision-guarded, and never into the critics. Committed (with the finalize empty-section guard + the task.md heading-order/hook fixes). _(quiz application later narrowed to feedback-only, above)_
- 2026-07-31 · **Equation renders plainly, not as literal source** — a bare `$…=…$` span (an equation the model delimited, no LaTeX command inside) was shipping with its dollar signs intact; a deterministic renderer pass now strips the delimiters, gated on a relation inside a math-only body so prose dollars (`$5 and $10`, a `$5 - $10` range) stay untouched, and running after the LaTeX passes so unresolved `\command` spans are still preserved for MathJax. `7569632`
- 2026-07-31 · **Versioned evaluation run-store** — each `evaluate` snapshots the run immutably to `<course>/evals/<timestamp>/` (review files + `summary.json` with the coursekit commit, model, and counts) and appends `evals/log.jsonl`, so quality is a readable trend, never an overwritten file; the top-level `*-review.md` stay as latest copies. `883e34f`
- 2026-07-31 · **Images on pages** — inline `ImageBlock` + `add_image`: the model places a `ref`/alt slot (never a URL), the instructor supplies the file via supplements, the renderer resolves it or shows a placeholder. `ecb26b3`
- 2026-07-31 · **Targeted fix loop (quiz + page)** — regenerate each flagged item in place + verify; from-review default (`parse_review`), a progress heartbeat, and the emit robustness guard (skip an out-of-sync week rather than KeyError). `024cc32`
- 2026-07-31 · **Canvas whole-course `.imscc` import — verified in real Canvas** (manual acceptance gate; pages + quizzes as week modules; no code commit).
- 2026-07-30 · **Concept map + the `analyze` phase** — per-week concept map (schema, `consolidate`, the `analyze` verb) from `knowledge.json`, with a text-fallback extractor for non-transcriber courses; wired into page generation (an un-skippable teaching checklist) + concept-delivery scoring. `838a12a`, `b755318`
- 2026-07-30 · **Content-relative page length + teaching structure** — task.md sizes the page to the concept count; one-idea-per-paragraph; each concept its own section. `e1ac724`
- 2026-07-29 · **Page-build hardening** — `add_code` `text` alias, columns multi-line code rendering, longer-content handling, finalize-stop dispatch (fixes the `--detail full` runaway). `0766f86`
- 2026-07-29 · **Concept-delivery evaluation** + `evaluate --all` / `--no-review`. `509d4ba`, `31d20ba`
- 2026-07-28 · **Page pedagogy rubric** — evaluate covers pages + pedagogy, critic domain-awareness, and the loop-back prompt fix (course pedagogy 9.25 → 13.5/15). `06ab78b`
- 2026-07-28 · **Page facticity review** + section scoring. `f55677a`
- 2026-07-27–28 · **Quiz evaluator** — cold-read critic, synthetic calibration, multi-read union, scorecards, reads param. `fe87d05`, `775ebe9`, `9f9a224`, `29840e5`, `b54c400`, `a770f99`
- 2026-07-31 · **Retrieval-foldout finalize gate** — `page.validate_final` requires a `details` block so foldouts stop being dropped (marked provisional; see the parking-lot note). `a58ed2c`
- 2026-07-31 · **Flexible quiz question count** — the quiz now reads the week's concept map and makes one group per teaching concept + one for the enduring understanding (variants drawn from each concept's knowledge components), replacing the hardcoded five; the model suggests the count from the map (or from the material when no map). Overrides at two levels: `questions:` on a week's `.vtconfig/concepts/week-N.yaml` (per-week, wins) and `questions:` in `.vtconfig/quiz.yaml` (per-course). Shared `concept_map.load_for_unit` used by both generators; `quiz/context._shape_directive`; task.md generalized. `ad54acc`

## Note

`agent/todo.md` predates this file; its live items should migrate here (`OPS-3`) so there is one forward view, not two.
