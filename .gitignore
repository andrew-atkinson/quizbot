# Python
__pycache__/
*.py[cod]
.pytest_cache

# shell
.venv
uv.lock
.env

# folders
# Track the markdown docs/guides; keep the heavy Canvas exports + samples out of git.
docs/*
!docs/*.md
input/
output/
reference/

# me only
setup.md

# Jupyter Notebooks
.ipynb_checkpoints/

# OS specific
.DS_Store

#claude
.claude
# generated by `coursekit evaluate` (fixture output, not source)
examples/**/quiz-review.md
# dumped by synthesize.write_fixtures — regenerable from synthesize.py, which is the source of truth
examples/synthetic/generated/
# saved scorecard runs (model-dependent run artifacts, not source)
evals/results/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
